---
phase: 01-foundation-and-data-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/db.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_db.py
autonomous: true
requirements: [DATA-03, DATA-04]

must_haves:
  truths:
    - "Configuration module exposes pinned model versions, noise parameters, paths, and seed registry"
    - "SQLite database can be created with the full RDD schema and accepts insert/query of experiment result rows"
    - "Seed registry provides deterministic seed derivation from (base_seed, prompt_id, noise_type, noise_level)"
  artifacts:
    - path: "src/config.py"
      provides: "Frozen dataclass config with pinned models, seeds, noise params, paths"
      exports: ["ExperimentConfig", "derive_seed"]
    - path: "src/db.py"
      provides: "SQLite schema creation and helper functions"
      exports: ["init_database", "insert_run", "query_runs"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
    - path: "tests/test_config.py"
      provides: "Config module tests"
    - path: "tests/test_db.py"
      provides: "Database module tests"
  key_links:
    - from: "src/db.py"
      to: "src/config.py"
      via: "imports ExperimentConfig for default paths"
      pattern: "from.*config.*import"
    - from: "src/db.py"
      to: "results/results.db"
      via: "creates SQLite database at configured path"
      pattern: "sqlite3\\.connect"
---

<objective>
Create the configuration module and SQLite database infrastructure for the Linguistic Tax research toolkit.

Purpose: Every other module depends on configuration constants (model versions, seeds, paths, noise parameters) and the results database schema. This is the foundation layer.
Output: `src/config.py` with frozen dataclass config + seed derivation, `src/db.py` with full RDD schema + helpers, shared test fixtures, and passing tests.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config module and shared test fixtures</name>
  <files>src/config.py, tests/conftest.py, tests/test_config.py</files>
  <read_first>
    - docs/RDD_Linguistic_Tax_v4.md (Section 4.1-4.3 for experiment parameters, Section 9.2 for schema field names)
    - CLAUDE.md (conventions: type hints, docstrings, logging not print, pinned model versions)
    - pyproject.toml (existing dependencies)
    - .planning/phases/01-foundation-and-data-infrastructure/01-RESEARCH.md (Pattern 4: Config as Frozen Dataclass, Pattern 1: Isolated Random Instances)
  </read_first>
  <behavior>
    - Test: ExperimentConfig is frozen (assigning attribute raises FrozenInstanceError)
    - Test: ExperimentConfig.claude_model == "claude-sonnet-4-20250514"
    - Test: ExperimentConfig.gemini_model == "gemini-1.5-pro"
    - Test: ExperimentConfig.base_seed == 42
    - Test: ExperimentConfig.type_a_rates == (0.05, 0.10, 0.20)
    - Test: ExperimentConfig.type_a_weights == (0.40, 0.25, 0.20, 0.15) [adj_swap, omission, doubling, transposition]
    - Test: ExperimentConfig.repetitions == 5
    - Test: ExperimentConfig.temperature == 0.0
    - Test: ExperimentConfig.prompts_path == "data/prompts.json"
    - Test: ExperimentConfig.matrix_path == "data/experiment_matrix.json"
    - Test: ExperimentConfig.results_db_path == "results/results.db"
    - Test: derive_seed(42, "humaneval_042", "type_a", "10") returns a deterministic int
    - Test: derive_seed called twice with same args returns same value
    - Test: derive_seed with different args returns different value
    - Test: derive_seed uses hashlib.sha256 (not global random state)
    - Test: NOISE_TYPES constant contains "clean", "type_a_5pct", "type_a_10pct", "type_a_20pct", "type_b_mandarin", "type_b_spanish", "type_b_japanese", "type_b_mixed"
    - Test: INTERVENTIONS constant contains "raw", "self_correct", "pre_proc_sanitize", "pre_proc_sanitize_compress", "prompt_repetition"
    - Test: MODELS constant contains "claude-sonnet-4-20250514", "gemini-1.5-pro"
  </behavior>
  <action>
    Create `src/config.py` with:

    1. A `@dataclass(frozen=True)` class `ExperimentConfig` with these exact fields and defaults:
       - `claude_model: str = "claude-sonnet-4-20250514"`
       - `gemini_model: str = "gemini-1.5-pro"`
       - `base_seed: int = 42`
       - `type_a_rates: tuple[float, ...] = (0.05, 0.10, 0.20)`
       - `type_a_weights: tuple[float, ...] = (0.40, 0.25, 0.20, 0.15)`
       - `repetitions: int = 5`
       - `temperature: float = 0.0`
       - `prompts_path: str = "data/prompts.json"`
       - `matrix_path: str = "data/experiment_matrix.json"`
       - `results_db_path: str = "results/results.db"`

    2. A function `derive_seed(base_seed: int, prompt_id: str, noise_type: str, noise_level: str) -> int` that:
       - Builds key string: `f"{base_seed}:{prompt_id}:{noise_type}:{noise_level}"`
       - Returns `int(hashlib.sha256(key.encode()).hexdigest()[:8], 16)`

    3. Module-level constants:
       - `NOISE_TYPES: tuple[str, ...] = ("clean", "type_a_5pct", "type_a_10pct", "type_a_20pct", "type_b_mandarin", "type_b_spanish", "type_b_japanese", "type_b_mixed")`
       - `INTERVENTIONS: tuple[str, ...] = ("raw", "self_correct", "pre_proc_sanitize", "pre_proc_sanitize_compress", "prompt_repetition")`
       - `MODELS: tuple[str, ...] = ("claude-sonnet-4-20250514", "gemini-1.5-pro")`

    4. Use `import logging` and create module logger: `logger = logging.getLogger(__name__)`

    Create `tests/conftest.py` with shared fixtures:
    - `sample_config()` -> returns `ExperimentConfig()` instance
    - `tmp_db_path(tmp_path)` -> returns `str(tmp_path / "test_results.db")`
    - `sample_prompt_record()` -> returns a dict with keys: benchmark_source, problem_id, prompt_text, canonical_answer, test_code, answer_type

    Create `tests/test_config.py` testing all behaviors listed above.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/config.py contains `@dataclass(frozen=True)` and `class ExperimentConfig`
    - src/config.py contains `def derive_seed(base_seed: int, prompt_id: str, noise_type: str, noise_level: str) -> int:`
    - src/config.py contains `NOISE_TYPES` with exactly 8 entries including "clean"
    - src/config.py contains `INTERVENTIONS` with exactly 5 entries
    - src/config.py contains `MODELS` with exactly 2 entries
    - src/config.py contains `import hashlib` (not `import random`)
    - src/config.py contains `import logging`
    - tests/conftest.py contains `def sample_config` and `def tmp_db_path`
    - tests/test_config.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>Config module exposes all pinned model versions, API settings, noise parameters, paths, and seed registry. derive_seed is deterministic using hashlib. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SQLite database module with full RDD schema</name>
  <files>src/db.py, tests/test_db.py</files>
  <read_first>
    - docs/RDD_Linguistic_Tax_v4.md (lines 1044-1101 for execution log schema and derived metrics schema)
    - src/config.py (just created - for ExperimentConfig import and path defaults)
    - CLAUDE.md (conventions)
    - .planning/phases/01-foundation-and-data-infrastructure/01-RESEARCH.md (SQLite schema code example)
  </read_first>
  <behavior>
    - Test: init_database creates a SQLite file at the given path
    - Test: experiment_runs table exists with columns: run_id, prompt_id, benchmark, noise_type, noise_level, intervention, model, repetition, prompt_text, prompt_tokens, optimized_tokens, raw_output, cot_trace, completion_tokens, pass_fail, ttft_ms, ttlt_ms, generation_ms, preproc_model, preproc_input_tokens, preproc_output_tokens, preproc_ttft_ms, preproc_ttlt_ms, main_model_input_cost_usd, main_model_output_cost_usd, preproc_cost_usd, total_cost_usd, temperature, timestamp, status
    - Test: derived_metrics table exists with columns: prompt_id, condition, model, consistency_rate, majority_pass, pass_count, quadrant, mean_ttft_ms, mean_ttlt_ms, mean_total_latency_ms, mean_total_cost_usd, token_savings, net_token_cost, std_latency_ms
    - Test: Indexes idx_runs_prompt, idx_runs_condition, idx_runs_status exist
    - Test: insert_run inserts a row and it can be queried back
    - Test: insert_run with duplicate run_id raises IntegrityError
    - Test: query_runs with filters returns matching rows only
    - Test: WAL journal mode is enabled after init
    - Test: init_database is idempotent (calling twice does not error)
  </behavior>
  <action>
    Create `src/db.py` with:

    1. Module-level `CREATE_SCHEMA` SQL string containing:
       - `CREATE TABLE IF NOT EXISTS experiment_runs (...)` with ALL columns from RDD Section 9.2:
         run_id TEXT PRIMARY KEY, prompt_id TEXT NOT NULL, benchmark TEXT NOT NULL, noise_type TEXT NOT NULL, noise_level TEXT, intervention TEXT NOT NULL, model TEXT NOT NULL, repetition INTEGER NOT NULL, prompt_text TEXT, prompt_tokens INTEGER, optimized_tokens INTEGER, raw_output TEXT, cot_trace TEXT, completion_tokens INTEGER, pass_fail INTEGER, ttft_ms REAL, ttlt_ms REAL, generation_ms REAL, preproc_model TEXT, preproc_input_tokens INTEGER, preproc_output_tokens INTEGER, preproc_ttft_ms REAL, preproc_ttlt_ms REAL, main_model_input_cost_usd REAL, main_model_output_cost_usd REAL, preproc_cost_usd REAL, total_cost_usd REAL, temperature REAL DEFAULT 0.0, timestamp TEXT, status TEXT DEFAULT 'pending'
       - `CREATE TABLE IF NOT EXISTS derived_metrics (...)` with columns:
         prompt_id TEXT NOT NULL, condition TEXT NOT NULL, model TEXT NOT NULL, consistency_rate REAL, majority_pass INTEGER, pass_count INTEGER, quadrant TEXT, mean_ttft_ms REAL, mean_ttlt_ms REAL, mean_total_latency_ms REAL, mean_total_cost_usd REAL, token_savings INTEGER, net_token_cost INTEGER, std_latency_ms REAL, PRIMARY KEY (prompt_id, condition, model)
       - Three indexes: idx_runs_prompt on experiment_runs(prompt_id), idx_runs_condition on experiment_runs(noise_type, intervention, model), idx_runs_status on experiment_runs(status)

    2. `def init_database(db_path: str) -> sqlite3.Connection` that:
       - Creates parent directories if needed using `pathlib.Path(db_path).parent.mkdir(parents=True, exist_ok=True)`
       - Calls `sqlite3.connect(db_path)`
       - Executes the CREATE_SCHEMA
       - Sets `PRAGMA journal_mode=WAL`
       - Sets `PRAGMA foreign_keys=ON`
       - Returns the connection

    3. `def insert_run(conn: sqlite3.Connection, run_data: dict) -> None` that:
       - Inserts a row into experiment_runs from a dict
       - Uses parameterized query (not string formatting)
       - Calls `conn.commit()`

    4. `def query_runs(conn: sqlite3.Connection, **filters) -> list[dict]` that:
       - Builds WHERE clause from keyword args (e.g., `prompt_id="humaneval_042"`)
       - Returns list of dicts (using `conn.row_factory = sqlite3.Row`)
       - Returns empty list if no matches

    5. Use `import logging` with `logger = logging.getLogger(__name__)`

    Create `tests/test_db.py` testing all behaviors listed above. Use `tmp_db_path` fixture from conftest.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_db.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/db.py contains `CREATE TABLE IF NOT EXISTS experiment_runs`
    - src/db.py contains `CREATE TABLE IF NOT EXISTS derived_metrics`
    - src/db.py contains `CREATE INDEX IF NOT EXISTS idx_runs_prompt`
    - src/db.py contains `CREATE INDEX IF NOT EXISTS idx_runs_condition`
    - src/db.py contains `CREATE INDEX IF NOT EXISTS idx_runs_status`
    - src/db.py contains `def init_database(db_path: str) -> sqlite3.Connection:`
    - src/db.py contains `def insert_run(conn: sqlite3.Connection, run_data: dict)`
    - src/db.py contains `def query_runs(conn: sqlite3.Connection`
    - src/db.py contains `PRAGMA journal_mode=WAL`
    - src/db.py contains `import logging`
    - tests/test_db.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>SQLite database module creates the full RDD schema with both tables and all indexes. insert_run and query_runs work correctly. WAL mode enabled. All tests pass.</done>
</task>

</tasks>

<verification>
All Phase 1 infrastructure foundation tests pass:
```bash
cd /home/steve/linguistic-tax && python -m pytest tests/test_config.py tests/test_db.py -v
```
</verification>

<success_criteria>
- ExperimentConfig frozen dataclass with all pinned values accessible
- derive_seed produces deterministic output from (base_seed, prompt_id, noise_type, noise_level)
- NOISE_TYPES, INTERVENTIONS, MODELS constants enumerate all experimental conditions
- SQLite schema matches RDD Section 9.2 with both experiment_runs and derived_metrics tables
- All helper functions (init_database, insert_run, query_runs) work correctly
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-infrastructure/01-01-SUMMARY.md`
</output>
