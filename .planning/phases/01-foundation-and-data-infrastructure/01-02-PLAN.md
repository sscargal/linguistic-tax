---
phase: 01-foundation-and-data-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/noise_generator.py
  - tests/test_noise_generator.py
autonomous: true
requirements: [NOISE-01, NOISE-02, NOISE-03, NOISE-04]

must_haves:
  truths:
    - "Type A noise at 5%, 10%, 20% error rates produces character mutations at approximately the target rate"
    - "Technical keywords (function names, variable names, operators, Python keywords, numbers) survive Type A mutation"
    - "Type B ESL patterns produce linguistically valid transformations for Mandarin, Spanish, Japanese, and Mixed L1 sources"
    - "Running the noise generator twice with the same seed produces byte-identical output"
    - "Noise generator is CLI-invocable with --input, --type, --rate, --seed arguments"
  artifacts:
    - path: "src/noise_generator.py"
      provides: "Type A and Type B noise injection with keyword protection and CLI interface"
      exports: ["inject_type_a_noise", "inject_type_b_noise", "build_adjacency_map", "ESLPattern"]
      min_lines: 200
    - path: "tests/test_noise_generator.py"
      provides: "Comprehensive noise generator tests including determinism"
      min_lines: 100
  key_links:
    - from: "src/noise_generator.py"
      to: "src/config.py"
      via: "imports derive_seed for deterministic seed computation"
      pattern: "from.*config.*import.*derive_seed"
    - from: "src/noise_generator.py"
      to: "random.Random(seed)"
      via: "isolated RNG instances per call"
      pattern: "random\\.Random\\("
---

<objective>
Build the noise generation module with Type A (character-level) and Type B (ESL syntactic) noise injection, keyword protection, determinism guarantees, and CLI interface.

Purpose: Noise generators are the core experimental manipulation. They must be deterministic (same seed = identical output), protect technical tokens, and produce linguistically valid noise patterns.
Output: `src/noise_generator.py` with full noise generation capability and CLI, plus comprehensive tests proving determinism and correctness.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-infrastructure/01-RESEARCH.md

<interfaces>
<!-- From src/config.py (created in Plan 01) -->
From src/config.py:
```python
def derive_seed(base_seed: int, prompt_id: str, noise_type: str, noise_level: str) -> int:
    """Deterministic seed derivation using SHA-256."""
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Type A character-level noise with keyword protection</name>
  <files>src/noise_generator.py, tests/test_noise_generator.py</files>
  <read_first>
    - docs/RDD_Linguistic_Tax_v4.md (lines 337-398 for Type A noise spec: 40% adjacent key swap, 25% char omission, 20% char doubling, 15% transposition; error rates 5/10/20%; keyword protection)
    - CLAUDE.md (conventions: type hints, docstrings, logging not print, fixed random seeds)
    - .planning/phases/01-foundation-and-data-infrastructure/01-RESEARCH.md (Pattern 1: Isolated Random Instances, Pattern 2: QWERTY Adjacent Key Map)
    - .planning/phases/01-foundation-and-data-infrastructure/01-CONTEXT.md (locked decisions on keyword protection and seed management)
    - src/config.py (for derive_seed function to import -- do NOT reimplement)
  </read_first>
  <behavior>
    - Test: build_adjacency_map returns dict where 'f' neighbors include 'd', 'g', 'r', 't', 'v', 'c'
    - Test: build_adjacency_map covers all 26 lowercase letters
    - Test: inject_type_a_noise with rate=0.0 returns input unchanged
    - Test: inject_type_a_noise with rate=0.05 on 200-char string modifies approx 10 chars (+/-5)
    - Test: inject_type_a_noise with rate=0.10 on 200-char string modifies approx 20 chars (+/-7)
    - Test: inject_type_a_noise with rate=0.20 on 200-char string modifies approx 40 chars (+/-10)
    - Test: inject_type_a_noise with same seed returns identical output on two calls
    - Test: inject_type_a_noise with different seeds returns different output
    - Test: inject_type_a_noise preserves Python keywords ("def", "return", "if", "else", "for", "while", "import", "class")
    - Test: inject_type_a_noise preserves function names matching `def \w+\(`
    - Test: inject_type_a_noise preserves numbers in math prompts (answer_type="numeric")
    - Test: inject_type_a_noise preserves operators (+, -, *, /, =, ==, !=)
    - Test: derive_seed (imported from config) returns same int for same args across calls
    - Test: derive_seed (imported from config) returns different int for different prompt_ids
  </behavior>
  <action>
    Create `src/noise_generator.py` with:

    1. Imports: `random, re, argparse, json, logging, pathlib` and `from dataclasses import dataclass`. Import derive_seed from config: `from config import derive_seed`. Set `logger = logging.getLogger(__name__)`.

    IMPORTANT: Do NOT reimplement derive_seed -- import it from src/config.py (created in Plan 01-01). The config module is the single source of truth for seed derivation.

    2. QWERTY adjacency: `QWERTY_ROWS = [list("qwertyuiop"), list("asdfghjkl"), list("zxcvbnm")]` with row offsets (0, 0.5, 1.0). `def build_adjacency_map() -> dict[str, list[str]]:` finds neighbors by computing 2D positions and finding keys within distance 1.5.

    3. Keyword protection constants:
       - `PYTHON_KEYWORDS: frozenset` = all Python reserved words
       - `OPERATORS: frozenset` = `{"+", "-", "*", "/", "=", "==", "!=", "<", ">", "<=", ">=", "**", "//", "%", "+=", "-=", "*=", "/=", "->", ":"}`
       - `def identify_protected_spans(text: str, answer_type: str) -> list[tuple[int, int]]:` that uses regex to find Python keywords (`\bkeyword\b`), function/variable names from `def \w+` and `\w+\(`, operators, and numbers (for answer_type=="numeric") as `\b\d+\.?\d*\b`.

    4. Mutation functions:
       - `def apply_adjacent_key_swap(char: str, rng: random.Random, adj_map: dict) -> str` -- picks random neighbor or returns char if none
       - `def apply_char_omission(char: str) -> str` -- returns ""
       - `def apply_char_doubling(char: str) -> str` -- returns char + char

    5. `def inject_type_a_noise(text: str, error_rate: float, seed: int, answer_type: str = "code") -> str:` that creates `rng = random.Random(seed)`, identifies protected spans, iterates chars, skips whitespace and protected spans, and for each mutable char with probability error_rate applies weighted mutation (0.40 adj_swap, 0.65 omission, 0.85 doubling, else transposition with next mutable char).

    Create `tests/test_noise_generator.py` with all behavior tests. Use test strings:
    - Code: `"def sort_list(items):\n    \"\"\"Sort the given list in ascending order and return it.\"\"\"\n    return sorted(items)"`
    - Math: `"If John has 5 apples and gives 2 to Mary, how many apples does John have left?"`

    Tests for derive_seed should import from config directly and verify behavior (these tests confirm the integration, not a local reimplementation).
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_noise_generator.py -x -v -k "type_a or adjacency or protected or determinism or derive_seed"</automated>
  </verify>
  <acceptance_criteria>
    - src/noise_generator.py contains `def inject_type_a_noise(text: str, error_rate: float, seed: int`
    - src/noise_generator.py contains `def build_adjacency_map() -> dict[str, list[str]]:`
    - src/noise_generator.py contains `def identify_protected_spans(text: str, answer_type: str) -> list[tuple[int, int]]:`
    - src/noise_generator.py contains `PYTHON_KEYWORDS` with at least "def", "return", "if", "for"
    - src/noise_generator.py contains `random.Random(seed)` (NOT `random.seed(`)
    - src/noise_generator.py contains `from config import derive_seed` (NOT a local reimplementation)
    - src/noise_generator.py contains `import logging` and does NOT contain `print(`
    - tests/test_noise_generator.py contains test for determinism
    - tests/test_noise_generator.py contains test for keyword protection
    - pytest exits 0 for all Type A tests
  </acceptance_criteria>
  <done>Type A noise generator produces character mutations at target error rates, protects technical keywords, and is deterministic. derive_seed is imported from config (single source of truth). All Type A tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Type B ESL syntactic noise and CLI interface</name>
  <files>src/noise_generator.py, tests/test_noise_generator.py</files>
  <read_first>
    - docs/RDD_Linguistic_Tax_v4.md (lines 401-423 for Type B ESL patterns)
    - src/noise_generator.py (current state from Task 1 -- MUST extend, not overwrite)
    - tests/test_noise_generator.py (current state from Task 1 -- MUST extend)
    - .planning/phases/01-foundation-and-data-infrastructure/01-RESEARCH.md (Pattern 3: Rule-Based ESL Templates, Pitfall 4)
    - .planning/phases/01-foundation-and-data-infrastructure/01-CONTEXT.md (locked: 5-8 patterns/L1, rule-based, one pass, mixed combines)
  </read_first>
  <behavior>
    - Test: ESLPattern dataclass has fields: name, l1_source, description, pattern, replacement
    - Test: MANDARIN_PATTERNS has 5-8 entries with l1_source=="mandarin"
    - Test: SPANISH_PATTERNS has 5-8 entries with l1_source=="spanish"
    - Test: JAPANESE_PATTERNS has 5-8 entries with l1_source=="japanese"
    - Test: inject_type_b_noise("Write a function that sorts the list", "mandarin") removes articles
    - Test: inject_type_b_noise with "spanish" produces preposition errors
    - Test: inject_type_b_noise with "japanese" alters sentence structure
    - Test: inject_type_b_noise with "mixed" applies patterns from multiple L1 sources
    - Test: inject_type_b_noise is deterministic (same input -> same output)
    - Test: CLI --type char --rate 0.10 --seed 42 processes input
    - Test: CLI --type esl --l1 mandarin processes input
  </behavior>
  <action>
    Extend `src/noise_generator.py` by appending (do NOT overwrite Task 1 code):

    1. `@dataclass` class `ESLPattern` with: `name: str`, `l1_source: str`, `description: str`, `pattern: str`, `replacement: str`

    2. `MANDARIN_PATTERNS: list[ESLPattern]` (5-8 entries, L1 transfer from Mandarin Chinese):
       - article_omission: `r"\b(a|an|the)\s+"` -> `""` (no article system)
       - tense_removal: `r"\b(was|were)\s+(\w+ing)\b"` -> `r"\2"` (no tense markers)
       - copula_omission: `r"\b(is|are)\s+(?=\w)"` -> `""` (copula often omitted)
       - preposition_simplification: `r"\binto\b"` -> `"to"` (preposition overgeneralization)
       - measure_word_insertion: add "one" before count nouns where missing

    3. `SPANISH_PATTERNS: list[ESLPattern]` (5-8 entries):
       - preposition_confusion: `r"\bdepend(?:s)?\s+on\b"` -> `"depend of"` ("depender de")
       - double_negative: `r"\bdon't\s+have\s+any\b"` -> `"don't have no"`
       - adjective_placement: swap adj-noun for color/size adjectives
       - ser_estar_confusion: `r"\b(is|are)\s+(happy|sad|tired|ready)\b"` -> `r"is being \2"`
       - gender_article: `r"\bthe\s+problem\b"` -> `"the problema"`

    4. `JAPANESE_PATTERNS: list[ESLPattern]` (5-8 entries):
       - topic_comment: add "As for X, ..." topic-prominence structure
       - article_omission: same as Mandarin
       - subject_omission: drop subject pronoun in imperative-like sentences
       - relative_clause_prenominal: move relative clauses before noun
       - plural_confusion: drop plural markers

    5. `def inject_type_b_noise(text: str, l1_source: str, seed: int | None = None) -> str:` that:
       - Maps l1_source to patterns: "mandarin"->MANDARIN, "spanish"->SPANISH, "japanese"->JAPANESE, "mixed"->all three combined
       - Applies each pattern once via `re.sub(..., flags=re.IGNORECASE)`
       - Returns transformed text (deterministic by design)

    6. CLI block `if __name__ == "__main__":` with argparse:
       - `--input`: path to JSON file with prompt records
       - `--type`: "char" or "esl" (required)
       - `--rate`: float for char type (default 0.10)
       - `--l1`: str for esl type (choices: mandarin, spanish, japanese, mixed)
       - `--seed`: int (default 42)
       - `--output`: output file path (optional)
       - Reads input, applies noise, writes output JSON

    Extend `tests/test_noise_generator.py` with Type B tests and CLI tests.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_noise_generator.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/noise_generator.py contains `class ESLPattern` with `l1_source: str`
    - src/noise_generator.py contains `MANDARIN_PATTERNS` with at least 5 entries
    - src/noise_generator.py contains `SPANISH_PATTERNS` with at least 5 entries
    - src/noise_generator.py contains `JAPANESE_PATTERNS` with at least 5 entries
    - src/noise_generator.py contains `def inject_type_b_noise(text: str, l1_source: str`
    - src/noise_generator.py contains `if __name__ == "__main__":`
    - src/noise_generator.py contains `argparse.ArgumentParser`
    - tests/test_noise_generator.py has tests for type_b patterns
    - tests/test_noise_generator.py has test for NOISE-04 determinism for both types
    - python -m pytest tests/test_noise_generator.py exits 0
  </acceptance_criteria>
  <done>Type B ESL noise produces valid transformations for all 4 L1 modes. CLI works for both char and esl. All tests pass including determinism (NOISE-04).</done>
</task>

</tasks>

<verification>
```bash
cd /home/steve/linguistic-tax && python -m pytest tests/test_noise_generator.py -v
```

Determinism smoke test:
```bash
cd /home/steve/linguistic-tax && python -c "
import sys; sys.path.insert(0, 'src')
from noise_generator import inject_type_a_noise, inject_type_b_noise
text = 'Write a function that sorts a list'
r1 = inject_type_a_noise(text, 0.10, 42)
r2 = inject_type_a_noise(text, 0.10, 42)
assert r1 == r2, 'Determinism failed!'
print('PASS')
"
```
</verification>

<success_criteria>
- Type A noise at 5%, 10%, 20% error rates with correct mutation weights
- Keyword protection for Python keywords, function names, operators, numbers
- Type B ESL with 5-8 patterns per L1 source (Mandarin, Spanish, Japanese)
- Mixed mode combines all L1 patterns
- Determinism verified for both noise types
- CLI interface with --type char/esl, --rate, --seed, --l1
- derive_seed imported from config.py (not reimplemented locally)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-infrastructure/01-02-SUMMARY.md`
</output>
