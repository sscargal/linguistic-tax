---
phase: 14-cli-config-subcommands-for-viewing-and-modifying-settings
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/test_config_commands.py
  - tests/test_cli.py
autonomous: true
requirements:
  - CFG-SHOW
  - CFG-SET
  - CFG-RESET
  - CFG-VALIDATE
  - CFG-DIFF
  - CFG-MODELS
  - CFG-ENTRY
  - CFG-COMPLETE

must_haves:
  truths:
    - "show-config displays all 13 ExperimentConfig properties in a table"
    - "show-config --json outputs valid parseable JSON"
    - "show-config --changed filters to only overridden properties"
    - "show-config with single property name prints just that value"
    - "set-config writes sparse overrides and coerces types correctly"
    - "set-config rejects invalid property names and invalid values"
    - "set-config auto-creates config file when none exists"
    - "reset-config removes single key from sparse config"
    - "reset-config --all deletes the config file"
    - "validate exits 0 for valid config and non-zero for invalid"
    - "diff shows only changed properties"
    - "list-models shows all PRICE_TABLE entries with pricing"
    - "build_cli registers all 7 subcommands"
  artifacts:
    - path: "tests/test_config_commands.py"
      provides: "Comprehensive tests for all 6 config subcommand handlers"
      min_lines: 150
    - path: "tests/test_cli.py"
      provides: "Updated CLI tests including new subcommand registration"
      contains: "test_build_cli_has_all_subcommands"
  key_links:
    - from: "tests/test_config_commands.py"
      to: "src/config_commands.py"
      via: "direct import of handler functions"
      pattern: "from src\\.config_commands import"
    - from: "tests/test_cli.py"
      to: "src/cli.py"
      via: "build_cli and parse_args"
      pattern: "from src\\.cli import build_cli"
---

<objective>
Write comprehensive tests for all 6 config subcommand handlers and updated CLI registration, ensuring every user-facing behavior from CONTEXT.md is verified.

Purpose: Validate that all config subcommands work correctly with the sparse override pattern, type coercion, validation, and proper output formatting.
Output: tests/test_config_commands.py (new), updated tests/test_cli.py
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-cli-config-subcommands-for-viewing-and-modifying-settings/14-CONTEXT.md
@.planning/phases/14-cli-config-subcommands-for-viewing-and-modifying-settings/14-01-SUMMARY.md

<interfaces>
<!-- From plan 14-01 outputs -->

From src/config_commands.py:
```python
FIELD_DESCRIPTIONS: dict[str, str]  # 13 entries mapping field name to description

def handle_show_config(args) -> None  # args.property, args.json, args.changed, args.verbose
def handle_set_config(args) -> None   # args.pairs: list[str]
def handle_reset_config(args) -> None # args.properties: list[str], args.all: bool
def handle_validate(args) -> None
def handle_diff(args) -> None
def handle_list_models(args) -> None
def property_name_completer(prefix, parsed_args, **kwargs) -> list[str]

def _load_raw_overrides(config_path=None) -> tuple[dict, Path]
def _coerce_value(field_name: str, raw_value: str)  # Returns typed value
def _format_value(value) -> str
```

From src/cli.py:
```python
def build_cli() -> argparse.ArgumentParser
# Subcommands: setup, show-config, set-config, reset-config, validate, diff, list-models
```

From src/config.py:
```python
class ExperimentConfig  # 13 fields, frozen=True
PRICE_TABLE  # 8 model entries
```

From src/config_manager.py:
```python
CONFIG_FILENAME = "experiment_config.json"
def find_config_path(start_dir: str = ".") -> Path | None
def load_config(config_path: Path | None = None) -> ExperimentConfig
def save_config(config: dict, config_path: Path | None = None) -> Path
def validate_config(config_dict: dict) -> list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_config_commands.py with comprehensive tests for all handlers</name>
  <files>tests/test_config_commands.py</files>
  <read_first>
    - src/config_commands.py (all handler signatures, _coerce_value, _load_raw_overrides, _format_value, FIELD_DESCRIPTIONS)
    - src/config.py (ExperimentConfig fields and defaults, PRICE_TABLE keys)
    - src/config_manager.py (CONFIG_FILENAME, save_config, load_config)
    - tests/test_config_manager.py (existing test patterns, tmp_path usage)
    - tests/test_cli.py (existing mock patterns)
  </read_first>
  <behavior>
    - test_show_config_table_all_properties: show-config with no flags prints table containing all 13 ExperimentConfig field names
    - test_show_config_modified_indicator: when config file has override, that property shows `*` prefix
    - test_show_config_json_output: --json flag produces valid JSON with all 13 keys
    - test_show_config_changed_filter: --changed with overrides only shows overridden properties
    - test_show_config_changed_no_overrides: --changed with no config file shows nothing
    - test_show_config_single_property: `show-config temperature` prints just `0.0`
    - test_show_config_single_property_json: `show-config temperature --json` prints `{"temperature": 0.0}`
    - test_show_config_single_property_invalid: unknown property name exits 1
    - test_show_config_verbose: --verbose adds Description column to output
    - test_set_config_creates_file: set-config on nonexistent config auto-creates the file
    - test_set_config_sparse_write: set-config writes only the set key, not all defaults
    - test_set_config_type_coercion_float: `temperature 0.5` coerces to float 0.5
    - test_set_config_type_coercion_int: `repetitions 3` coerces to int 3
    - test_set_config_type_coercion_tuple: `type_a_rates 0.05,0.10,0.30` coerces to tuple of floats
    - test_set_config_multiple_pairs: `temperature 0.5 repetitions 3` sets both
    - test_set_config_validates_before_save: invalid model name exits 1 without writing
    - test_set_config_unknown_property: unknown key exits 1
    - test_set_config_odd_pairs: odd number of args exits 1
    - test_set_config_change_summary: output contains `temperature: 0.0 -> 0.5`
    - test_reset_config_single: reset-config removes key from sparse config
    - test_reset_config_preserves_other: resetting one key preserves other overrides
    - test_reset_config_already_default: resetting non-overridden key prints "already at default"
    - test_reset_config_all: --all deletes the config file
    - test_reset_config_unknown_property: unknown property exits 1
    - test_validate_valid: validate exits 0 with valid config
    - test_validate_invalid: validate exits 1 with invalid model name
    - test_validate_no_config: validate with no config file exits 0 (defaults are valid)
    - test_diff_no_changes: diff with no overrides prints "No changes from defaults"
    - test_diff_with_changes: diff shows changed properties with default and current values
    - test_list_models_all_entries: list-models output contains all 8 PRICE_TABLE model names
    - test_list_models_free_indicator: output contains "free" for openrouter models
    - test_coerce_value_string: _coerce_value for str field returns string
    - test_property_name_completer: completer returns matching field names for prefix
  </behavior>
  <action>
Create `tests/test_config_commands.py` with pytest tests.

**Test infrastructure:**
- Use `tmp_path` fixture for all config file operations
- Use `monkeypatch.chdir(tmp_path)` to isolate config file discovery to tmp directory
- Create an `argparse.Namespace` mock for args objects:
```python
from types import SimpleNamespace
def make_args(**kwargs):
    """Create a Namespace with defaults for all handler args."""
    defaults = {"property": None, "json": False, "changed": False, "verbose": False,
                "pairs": [], "properties": [], "all": False}
    defaults.update(kwargs)
    return SimpleNamespace(**defaults)
```
- Use `capsys` to capture printed output
- For tests that need a config file, write it with `json.dump` directly to `tmp_path / "experiment_config.json"`

**show-config tests:**
- `test_show_config_table_all_properties`: call `handle_show_config(make_args())` in tmp_path, capture output, assert all 13 field names appear in output (e.g., `assert "temperature" in output`, `assert "claude_model" in output`, etc.)
- `test_show_config_modified_indicator`: write `{"temperature": 0.5}` to config file, call handler, assert `"*temperature"` appears in output
- `test_show_config_json_output`: call with `make_args(json=True)`, parse output as JSON, assert 13 keys present
- `test_show_config_changed_filter`: write `{"temperature": 0.5}` to config, call with `make_args(changed=True)`, assert `"temperature"` in output and `"repetitions"` NOT in output
- `test_show_config_changed_no_overrides`: no config file, `make_args(changed=True)`, assert output is just the header or empty table
- `test_show_config_single_property`: `make_args(property="temperature")`, assert output.strip() == "0.0"
- `test_show_config_single_property_json`: `make_args(property="temperature", json=True)`, assert `json.loads(output) == {"temperature": 0.0}`
- `test_show_config_single_property_invalid`: `make_args(property="nonexistent")`, assert SystemExit with code 1
- `test_show_config_verbose`: `make_args(verbose=True)`, assert "Description" in output or a field description string appears

**set-config tests:**
- `test_set_config_creates_file`: no config file, call `handle_set_config(make_args(pairs=["temperature", "0.5"]))`, assert config file now exists
- `test_set_config_sparse_write`: set temperature, read raw JSON, assert only `{"temperature": 0.5}` (no other keys)
- `test_set_config_type_coercion_float`: set temperature to "0.5", load config, assert `config.temperature == 0.5`
- `test_set_config_type_coercion_int`: set repetitions to "3", load config, assert `config.repetitions == 3`
- `test_set_config_type_coercion_tuple`: set type_a_rates to "0.05,0.10,0.30", load config, assert `config.type_a_rates == (0.05, 0.10, 0.30)`
- `test_set_config_multiple_pairs`: `pairs=["temperature", "0.5", "repetitions", "3"]`, load config, assert both changed
- `test_set_config_validates_before_save`: `pairs=["claude_model", "invalid-model"]`, assert SystemExit(1), assert no config file written
- `test_set_config_unknown_property`: `pairs=["nonexistent", "value"]`, assert SystemExit(1)
- `test_set_config_odd_pairs`: `pairs=["temperature"]`, assert SystemExit(1)
- `test_set_config_change_summary`: capture output, assert `"temperature: 0.0 -> 0.5"` in output (using `->` or `→`)

**reset-config tests:**
- `test_reset_config_single`: write `{"temperature": 0.5, "repetitions": 3}`, reset temperature, read JSON, assert only `{"repetitions": 3}`
- `test_reset_config_preserves_other`: same as above, verify repetitions still in file
- `test_reset_config_already_default`: no override for temperature, call reset, assert "already at default" in output
- `test_reset_config_all`: write config, `make_args(all=True, properties=[])`, assert config file deleted
- `test_reset_config_unknown_property`: `make_args(properties=["nonexistent"])`, assert SystemExit(1)

**validate tests:**
- `test_validate_valid`: write `{"temperature": 0.5}`, call handler, no SystemExit (or exit 0)
- `test_validate_invalid`: write `{"claude_model": "invalid"}`, assert SystemExit(1)
- `test_validate_no_config`: no file, call handler, no SystemExit (defaults valid)

**diff tests:**
- `test_diff_no_changes`: no config file, assert "No changes from defaults" in output
- `test_diff_with_changes`: write `{"temperature": 0.5}`, assert "temperature" in output and "0.0" in output and "0.5" in output

**list-models tests:**
- `test_list_models_all_entries`: call handler, assert all 8 PRICE_TABLE keys appear in output
- `test_list_models_free_indicator`: assert "free" appears in output (for openrouter models)

**Helper tests:**
- `test_coerce_value_string`: `_coerce_value("prompts_path", "custom/path.json")` returns `"custom/path.json"`
- `test_property_name_completer`: `property_name_completer("temp", None)` returns `["temperature"]`
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_config_commands.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_config_commands.py exists with at least 25 test functions
    - tests/test_config_commands.py contains `from src.config_commands import`
    - tests/test_config_commands.py contains `test_show_config_table_all_properties`
    - tests/test_config_commands.py contains `test_set_config_sparse_write`
    - tests/test_config_commands.py contains `test_reset_config_single`
    - tests/test_config_commands.py contains `test_validate_valid`
    - tests/test_config_commands.py contains `test_diff_with_changes`
    - tests/test_config_commands.py contains `test_list_models_all_entries`
    - tests/test_config_commands.py contains `test_set_config_type_coercion_tuple`
    - `python -m pytest tests/test_config_commands.py -x -q` exits 0
  </acceptance_criteria>
  <done>All config subcommand handlers have comprehensive test coverage, all tests pass, and the sparse override pattern is verified end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_cli.py with new subcommand registration tests</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py (current test functions)
    - src/cli.py (updated build_cli with all subcommands)
  </read_first>
  <behavior>
    - test_build_cli_has_all_subcommands: parse_args for each of the 7 subcommands succeeds
    - test_build_cli_prog_name_is_propt: parser.prog == "propt"
    - test_show_config_parser_flags: show-config accepts --json, --changed, --verbose, optional property
    - test_set_config_parser_pairs: set-config accepts variable number of positional args
    - test_reset_config_parser_all_flag: reset-config accepts --all flag and property names
    - test_main_show_config_routes: main with "show-config" calls handle_show_config
    - test_main_set_config_routes: main with "set-config temperature 0.5" calls handle_set_config
  </behavior>
  <action>
Add the following tests to `tests/test_cli.py`, keeping all existing tests intact:

**`test_build_cli_has_all_subcommands`:**
- Import build_cli, create parser
- For each command in ["setup", "show-config", "set-config", "reset-config", "validate", "diff", "list-models"]:
  - If command is "set-config": parse_args([command, "key", "val"])
  - Elif command is "reset-config": parse_args([command, "key"])
  - Else: parse_args([command])
  - Assert args.command == command

**`test_build_cli_prog_name_is_propt`:**
- parser = build_cli()
- assert parser.prog == "propt"

**`test_show_config_parser_flags`:**
- parser = build_cli()
- args = parser.parse_args(["show-config", "--json", "--changed", "--verbose"])
- assert args.json is True, args.changed is True, args.verbose is True
- args2 = parser.parse_args(["show-config", "temperature"])
- assert args2.property == "temperature"

**`test_set_config_parser_pairs`:**
- parser = build_cli()
- args = parser.parse_args(["set-config", "temperature", "0.5", "repetitions", "3"])
- assert args.pairs == ["temperature", "0.5", "repetitions", "3"]

**`test_reset_config_parser_all_flag`:**
- parser = build_cli()
- args = parser.parse_args(["reset-config", "--all"])
- assert args.all is True
- args2 = parser.parse_args(["reset-config", "temperature", "repetitions"])
- assert args2.properties == ["temperature", "repetitions"]

**`test_main_show_config_routes`:**
- Patch sys.argv to ["propt", "show-config"]
- Patch handle_show_config
- Call main(), assert handle_show_config called once

**`test_main_set_config_routes`:**
- Patch sys.argv to ["propt", "set-config", "temperature", "0.5"]
- Patch handle_set_config
- Call main(), assert handle_set_config called once
- Assert args.pairs == ["temperature", "0.5"]
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_cli.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cli.py contains `test_build_cli_has_all_subcommands`
    - tests/test_cli.py contains `test_build_cli_prog_name_is_propt`
    - tests/test_cli.py contains `test_show_config_parser_flags`
    - tests/test_cli.py contains `test_set_config_parser_pairs`
    - tests/test_cli.py contains `test_reset_config_parser_all_flag`
    - tests/test_cli.py contains `test_main_show_config_routes`
    - tests/test_cli.py still contains `test_build_cli_has_setup_subcommand` (existing test preserved)
    - tests/test_cli.py still contains `test_main_setup_routes_to_wizard` (existing test preserved)
    - `python -m pytest tests/test_cli.py -x -q` exits 0
  </acceptance_criteria>
  <done>CLI tests cover all 7 subcommands, argument parsing, routing, and the propt prog name. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_config_commands.py tests/test_cli.py -x -q` exits 0
- `python -m pytest tests/ -x -q` exits 0 (full suite still passes)
</verification>

<success_criteria>
- tests/test_config_commands.py has 25+ tests covering all 6 handlers
- tests/test_cli.py has 7+ new tests for subcommand registration and routing
- All tests pass with `pytest tests/test_config_commands.py tests/test_cli.py -x -q`
- Full test suite `pytest tests/ -x -q` still passes
- Sparse override pattern is verified (set-config only writes overrides, not full config)
- Type coercion verified for float, int, tuple, and string
- Invalid inputs properly rejected with exit code 1
</success_criteria>

<output>
After completion, create `.planning/phases/14-cli-config-subcommands-for-viewing-and-modifying-settings/14-02-SUMMARY.md`
</output>
