---
phase: 17-registry-consumers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/compute_derived.py
  - src/run_experiment.py
  - src/pilot.py
  - src/setup_wizard.py
  - scripts/generate_matrix.py
  - tests/test_matrix.py
  - tests/test_setup_wizard.py
  - tests/test_pilot.py
autonomous: true
requirements: [EXP-01, EXP-03, EXP-04]

must_haves:
  truths:
    - "Experiment matrix generation uses registry.target_models() not hardcoded MODELS tuple"
    - "generate_matrix.py accepts --models CLI override for subset generation"
    - "pilot.py derives _VALID_MODELS from registry, accepting custom configured models"
    - "compute_derived.py iterates registry.target_models() not hardcoded MODELS"
    - "run_experiment.py validates --model against registry not hardcoded choices"
    - "setup_wizard.py builds PROVIDERS from registry not MODELS import"
    - "Full 541-test suite passes after each module migration"
  artifacts:
    - path: "scripts/generate_matrix.py"
      provides: "Registry-based matrix generation with --models override"
      contains: "registry.target_models()"
    - path: "src/pilot.py"
      provides: "Registry-derived valid models set"
      contains: "registry.target_models()"
    - path: "src/compute_derived.py"
      provides: "Registry-based model iteration for derived metrics"
      contains: "registry.target_models()"
    - path: "src/run_experiment.py"
      provides: "Registry-validated model flag"
      contains: "from src.model_registry import registry"
    - path: "src/setup_wizard.py"
      provides: "Registry-based provider/model discovery"
      contains: "from src.model_registry import registry"
  key_links:
    - from: "scripts/generate_matrix.py"
      to: "src/model_registry.py"
      via: "registry.target_models() for model list"
      pattern: "registry\\.target_models"
    - from: "src/pilot.py"
      to: "src/model_registry.py"
      via: "set(registry.target_models()) for validation"
      pattern: "set\\(registry\\.target_models"
    - from: "src/compute_derived.py"
      to: "src/model_registry.py"
      via: "registry.target_models() iteration"
      pattern: "registry\\.target_models"
    - from: "src/run_experiment.py"
      to: "src/model_registry.py"
      via: "registry for model validation and compute_cost"
      pattern: "registry\\.(compute_cost|target_models)"
---

<objective>
Migrate five pipeline consumer modules (compute_derived, run_experiment, pilot, generate_matrix, setup_wizard) from config.py shim imports to direct model_registry usage. Add --models CLI override to generate_matrix.py.

Purpose: These modules are the core experiment pipeline. After migration, custom models configured via the registry flow through matrix generation, execution, pilot runs, and derived metrics without hitting allowlist rejections. This addresses EXP-01, EXP-03, and EXP-04 directly.
Output: Five source modules and their tests using registry directly, --models flag on generate_matrix.py, 541 tests still passing.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-registry-consumers/17-CONTEXT.md
@.planning/phases/17-registry-consumers/17-RESEARCH.md

<interfaces>
<!-- Registry API from src/model_registry.py (Phase 16) -->

```python
registry: ModelRegistry = _build_registry()

class ModelRegistry:
    _models: dict[str, ModelConfig]  # keyed by model_id

    def get_price(self, model_id: str) -> tuple[float, float]: ...
    def get_preproc(self, model_id: str) -> str | None: ...
    def get_delay(self, model_id: str) -> float: ...
    def target_models(self) -> list[str]: ...
    def compute_cost(self, model_id: str, input_tokens: int, output_tokens: int) -> float: ...
    def reload(self, models: list[ModelConfig]) -> None: ...
```

<!-- config.py exports that STAY (not shims, keep importing these): -->
```python
# These are NOT shims and remain in config.py:
ExperimentConfig  # dataclass
derive_seed       # function
INTERVENTIONS     # tuple of strings
NOISE_TYPES       # tuple of strings
MAX_TOKENS_BY_BENCHMARK  # dict
OPENROUTER_BASE_URL      # string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate compute_derived.py, pilot.py, and run_experiment.py to registry</name>
  <files>src/compute_derived.py, src/pilot.py, src/run_experiment.py, tests/test_pilot.py</files>
  <read_first>
    - src/compute_derived.py (line 22 imports MODELS; line ~482 iterates `for model in MODELS`)
    - src/pilot.py (line 22 imports MODELS; line ~274 has `_VALID_MODELS = set(MODELS)`)
    - src/run_experiment.py (line 21 imports PREPROC_MODEL_MAP, compute_cost; line ~551 has choices=["claude", "gemini", "all"])
    - tests/test_pilot.py (line 729 imports MODELS)
    - tests/test_run_experiment.py (check for model-related imports/assertions)
    - src/model_registry.py (registry API)
  </read_first>
  <action>
**compute_derived.py migration (EXP-04):**
1. Replace `from src.config import INTERVENTIONS, MODELS, NOISE_TYPES` with `from src.config import INTERVENTIONS, NOISE_TYPES` and add `from src.model_registry import registry`
2. Replace every `MODELS` reference in the file with `registry.target_models()`. Key location: the `for model in MODELS:` loop (around line 482 in main()). Also check for any other MODELS references in the file (e.g., in constants or tuple unpacking).
3. Note: INTERVENTIONS and NOISE_TYPES stay imported from config.py (they are not model-related).

**pilot.py migration (EXP-03):**
1. Replace `from src.config import ExperimentConfig, MODELS, derive_seed` with `from src.config import ExperimentConfig, derive_seed` and add `from src.model_registry import registry`
2. Replace `_VALID_MODELS = set(MODELS)` (around line 274) with `_VALID_MODELS = set(registry.target_models())`
3. Search for any other MODELS references in pilot.py and replace with `registry.target_models()`.
4. Update tests/test_pilot.py: replace `from src.config import MODELS` (line 729) with `from src.model_registry import registry` and update the assertion to use `registry.target_models()`.

**run_experiment.py migration (EXP-02):**
1. Replace import: remove `PREPROC_MODEL_MAP` and `compute_cost` from the `from src.config import (...)` block. Add `from src.model_registry import registry`.
2. Replace `PREPROC_MODEL_MAP[model]` or `.get(model)` calls with `registry.get_preproc(model)`.
3. Replace `compute_cost(model, inp, out)` calls with `registry.compute_cost(model, inp, out)`.
4. In `_build_parser()` (around line 551): remove `choices=["claude", "gemini", "all"]` from the `--model` argument. Instead, accept any string. The model argument validation should happen after parsing:
   - If value is "all" -> run all configured target models
   - If value matches a full model_id in registry -> filter to that model
   - If value matches a provider prefix (e.g., "claude", "gemini", "openai", "openrouter") -> filter target_models by prefix
   - Otherwise -> raise argparse error with helpful message listing available models
5. Keep ExperimentConfig, derive_seed, INTERVENTIONS, NOISE_TYPES, MAX_TOKENS_BY_BENCHMARK imports from config.py.

**Commit each module migration separately** with message format: `refactor(17): migrate {module} to model_registry`

Run `pytest tests/ -x -q` after each commit to verify 541 tests pass.
  </action>
  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/compute_derived.py contains `from src.model_registry import registry`
    - src/compute_derived.py does NOT contain `import.*MODELS` (as a config import)
    - src/compute_derived.py contains `registry.target_models()`
    - src/pilot.py contains `from src.model_registry import registry`
    - src/pilot.py does NOT contain `import.*MODELS` (as a config import)
    - src/pilot.py contains `set(registry.target_models())`
    - src/run_experiment.py contains `from src.model_registry import registry`
    - src/run_experiment.py does NOT contain `PREPROC_MODEL_MAP` or `import.*compute_cost` from config
    - src/run_experiment.py contains `registry.compute_cost(`
    - src/run_experiment.py does NOT contain `choices=["claude", "gemini", "all"]`
    - tests/test_pilot.py does NOT contain `from src.config import MODELS`
    - `pytest tests/ -x -q` exits 0 with 541 tests passed
  </acceptance_criteria>
  <done>compute_derived.py, pilot.py, and run_experiment.py use registry directly. Custom models flow through matrix generation, pilot validation, experiment execution, and derived metrics. 541 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate generate_matrix.py and setup_wizard.py to registry</name>
  <files>scripts/generate_matrix.py, src/setup_wizard.py, tests/test_matrix.py, tests/test_setup_wizard.py</files>
  <read_first>
    - scripts/generate_matrix.py (line 17 imports MODELS from config; sys.path manipulation for imports)
    - src/setup_wizard.py (line 18 imports MODELS, PREPROC_MODEL_MAP; PROVIDERS dict built from MODELS)
    - tests/test_matrix.py (line 9 imports MODELS from config)
    - tests/test_setup_wizard.py (line 39 imports MODELS; line 238 imports PREPROC_MODEL_MAP)
    - src/model_registry.py (registry API)
  </read_first>
  <action>
**generate_matrix.py migration (EXP-01):**
1. Replace `from config import INTERVENTIONS, MODELS, NOISE_TYPES, ExperimentConfig` with `from config import INTERVENTIONS, NOISE_TYPES, ExperimentConfig` and add `from model_registry import registry` (note: scripts/ uses sys.path trick, so no `src.` prefix).
2. Replace every `MODELS` reference with `registry.target_models()`. Key location: the models loop in `generate_matrix()` function.
3. Add `--models` CLI argument to the argparse section:
   ```python
   parser.add_argument(
       "--models",
       type=str,
       default=None,
       help="Comma-separated model IDs (default: all configured target models)",
   )
   ```
4. In main/generate function, use the --models override:
   ```python
   models = args.models.split(",") if args.models else None
   # Pass models to generate_matrix; if None, function uses registry.target_models()
   ```
5. Update the `generate_matrix()` function signature to accept an optional `models` parameter:
   ```python
   def generate_matrix(prompts_path, config=None, models=None):
       if models is None:
           models = registry.target_models()
       # ... use models in existing loops ...
   ```
6. Update tests/test_matrix.py: replace `from src.config import INTERVENTIONS, MODELS, NOISE_TYPES` with `from src.config import INTERVENTIONS, NOISE_TYPES` and `from src.model_registry import registry`. Replace MODELS references with `registry.target_models()`.

**setup_wizard.py migration:**
1. Replace `from src.config import MODELS, OPENROUTER_BASE_URL, PREPROC_MODEL_MAP` with `from src.config import OPENROUTER_BASE_URL` and add `from src.model_registry import registry`
2. Replace `MODELS` references: wherever PROVIDERS dict is built by filtering MODELS by prefix, use `registry.target_models()` instead.
3. Replace `PREPROC_MODEL_MAP` references with `registry.get_preproc(model_id)`.
4. Update tests/test_setup_wizard.py: replace `from src.config import MODELS` (line 39) with `from src.model_registry import registry` and `registry.target_models()`. Replace `from src.config import PREPROC_MODEL_MAP` (line 238) with `registry.get_preproc()`.

**Commit each module migration separately** with message format: `refactor(17): migrate {module} to model_registry`

Run `pytest tests/ -x -q` after each commit to verify 541 tests pass.
  </action>
  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_matrix.py contains `from model_registry import registry`
    - scripts/generate_matrix.py does NOT contain `import.*MODELS` (from config)
    - scripts/generate_matrix.py contains `registry.target_models()`
    - scripts/generate_matrix.py contains `--models` in argparse section
    - src/setup_wizard.py contains `from src.model_registry import registry`
    - src/setup_wizard.py does NOT contain `import.*MODELS` or `import.*PREPROC_MODEL_MAP` from config
    - tests/test_matrix.py does NOT contain `from src.config import.*MODELS`
    - tests/test_setup_wizard.py does NOT contain `from src.config import MODELS`
    - `pytest tests/ -x -q` exits 0 with 541 tests passed
  </acceptance_criteria>
  <done>generate_matrix.py uses registry with --models override. setup_wizard.py uses registry for model/provider discovery. 541 tests pass.</done>
</task>

</tasks>

<verification>
- All five pipeline modules import from model_registry, not config shims
- generate_matrix.py supports --models CLI flag
- run_experiment.py accepts any configured model via --model (no hardcoded choices)
- pilot.py accepts custom configured models
- compute_derived.py iterates configured models only
- Full 541-test suite passes
</verification>

<success_criteria>
- grep -r "from src.config import.*MODELS\b" src/compute_derived.py src/pilot.py src/run_experiment.py src/setup_wizard.py returns no matches
- grep -r "from config import.*MODELS\b" scripts/generate_matrix.py returns no matches
- grep "registry.target_models" src/compute_derived.py src/pilot.py scripts/generate_matrix.py returns 3+ matches
- scripts/generate_matrix.py contains "--models" argument
- pytest tests/ -x -q exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-registry-consumers/17-02-SUMMARY.md`
</output>
