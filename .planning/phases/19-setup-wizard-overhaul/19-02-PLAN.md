---
phase: 19-setup-wizard-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - tests/test_setup_wizard.py
autonomous: true
requirements:
  - WIZ-01
  - WIZ-02
  - WIZ-03
  - WIZ-04
  - WIZ-05
  - WIZ-06
  - DSC-03

must_haves:
  truths:
    - "Tests verify multi-provider selection parses comma-separated input"
    - "Tests verify free-text model entry accepts any string"
    - "Tests verify .env write + os.environ update during key collection"
    - "Tests verify validate_api_key uses provided model_id"
    - "Tests verify budget preview includes pilot and full estimates"
    - "Tests verify target vs preproc explanation is printed"
    - "Tests verify Ctrl+C at each wizard phase cancels cleanly"
    - "Tests verify existing config detection triggers re-entry menu"
  artifacts:
    - path: "tests/test_setup_wizard.py"
      provides: "Comprehensive test suite for rewritten wizard"
      min_lines: 200
  key_links:
    - from: "tests/test_setup_wizard.py"
      to: "src/setup_wizard.py"
      via: "imports and exercises all public and key private functions"
      pattern: "from src.setup_wizard import"
---

<objective>
Rewrite tests/test_setup_wizard.py to comprehensively cover the new multi-provider wizard flow, replacing the old single-provider tests.

Purpose: The wizard rewrite changes every function signature and flow. Existing 18 tests are all invalid. New tests must cover all 7 requirements plus edge cases.
Output: Rewritten tests/test_setup_wizard.py with 25+ tests covering all wizard functions and flows.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-setup-wizard-overhaul/19-CONTEXT.md
@.planning/phases/19-setup-wizard-overhaul/19-01-SUMMARY.md

<interfaces>
<!-- From Plan 01's rewritten src/setup_wizard.py -->
<!-- Executor MUST read src/setup_wizard.py to see actual signatures before writing tests -->

Expected public API:
```python
def run_setup_wizard(args: Any, input_fn: Callable[..., str] | None = None) -> None: ...
def check_environment() -> list[tuple[str, bool, str]]: ...
def validate_api_key(provider: str, env_var: str, model_id: str | None = None) -> tuple[bool, str]: ...
```

Expected private helpers (all take input_fn where user input needed):
```python
def _mask_key(key: str) -> str: ...
def _parse_provider_selection(raw: str, available: list[str]) -> list[str]: ...
def _detect_existing_config() -> dict | None: ...
def _handle_existing_config(existing: dict, input_fn: Callable) -> str: ...
def _select_providers(input_fn: Callable, existing_providers: list[str] | None = None) -> list[str]: ...
def _collect_api_keys(providers: list[str], input_fn: Callable, env_path: Path | None = None) -> dict[str, str]: ...
def _explain_model_roles(input_fn: Callable) -> str: ...
def _select_models(providers: list[str], preproc_scope: str, input_fn: Callable, existing_models: dict | None = None) -> list[dict]: ...
def _browse_models(provider: str, input_fn: Callable, timeout: float = 5.0) -> str | None: ...
def _validate_models(models: list[dict], input_fn: Callable) -> list[dict]: ...
def _build_budget_preview(models: list[dict]) -> str: ...
def _show_confirmation(models: list[dict], keys: dict[str, str], input_fn: Callable) -> bool: ...
def _build_config_dict(models: list[dict]) -> dict: ...
```

Constants:
```python
PROVIDER_NAMES: dict[str, str]
PROVIDER_ORDER: list[str]  # ["anthropic", "google", "openai", "openrouter"]
PROVIDER_ENV_VARS: dict[str, str]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite unit tests for wizard helper functions</name>
  <files>tests/test_setup_wizard.py</files>
  <read_first>
    - tests/test_setup_wizard.py (current tests being replaced)
    - src/setup_wizard.py (actual implementation to test -- read to confirm exact signatures)
    - src/env_manager.py (write_env signature for mock verification)
    - src/model_discovery.py (_query_anthropic signature for mock setup)
  </read_first>
  <action>
Rewrite `tests/test_setup_wizard.py` completely. Use pytest with `unittest.mock.patch` and `unittest.mock.MagicMock`. Use `tmp_path` fixture for any file operations. All tests use `input_fn` injection (no monkeypatching builtins.input).

**Test sections and specific tests to write:**

### Section 1: Helper function tests

1. `test_mask_key_long_key()` -- `_mask_key("sk-ant-api03-abcdef123456")` returns `"sk-ant...3456"`.
2. `test_mask_key_short_key()` -- `_mask_key("abc")` returns `"****"`.
3. `test_mask_key_exactly_ten_chars()` -- `_mask_key("1234567890")` returns `"****"`.
4. `test_parse_provider_selection_valid()` -- `_parse_provider_selection("1,3", ["a","b","c","d"])` returns `["a","c"]`.
5. `test_parse_provider_selection_invalid_defaults_to_first()` -- `_parse_provider_selection("99", ["a","b"])` returns `["a"]`.
6. `test_parse_provider_selection_empty_defaults_to_first()` -- `_parse_provider_selection("", ["a","b"])` returns `["a"]`.
7. `test_parse_provider_selection_duplicate_deduplication()` -- `_parse_provider_selection("1,1,2", ["a","b","c"])` handles duplicates (returns `["a","b"]` or `["a","a","b"]` -- check actual implementation and match).

### Section 2: Provider selection tests

8. `test_select_providers_single()` -- Mock `input_fn` returning "1". Verify returns `["anthropic"]`.
9. `test_select_providers_multiple()` -- Mock `input_fn` returning "1,2,4". Verify returns `["anthropic","google","openrouter"]`.
10. `test_select_providers_marks_existing()` -- Pass `existing_providers=["anthropic"]`. Capture stdout. Verify "(configured)" appears in output.

### Section 3: Key collection tests (WIZ-04)

11. `test_collect_api_keys_new_key_writes_env(tmp_path)` -- Patch `os.environ` to empty. Mock input returning "test-key-123". Patch `write_env`. Verify `write_env` called with correct env_var and value. Verify `os.environ[env_var]` set to "test-key-123" after call.
12. `test_collect_api_keys_keeps_existing_key()` -- Patch `os.environ` with `{"ANTHROPIC_API_KEY": "existing"}`. Mock input returning "" (accept default/keep). Verify `write_env` NOT called. Verify returned dict has the existing key.
13. `test_collect_api_keys_replaces_existing_key(tmp_path)` -- Patch `os.environ` with existing key. Mock input returning "n" then "new-key". Patch `write_env`. Verify `write_env` called with new key.

### Section 4: Model role explanation tests (WIZ-01)

14. `test_explain_model_roles_prints_explanation(capsys)` -- Mock input returning "1" (per-provider). Capture stdout. Verify "Target models are the LLMs" in output. Verify returns "per-provider".
15. `test_explain_model_roles_global_preproc()` -- Mock input returning "2". Verify returns "global".

### Section 5: Model selection tests (WIZ-03, DSC-03)

16. `test_select_models_accepts_default()` -- Mock input returning "" (accept default) for target, "" for preproc. Verify returned model entry has the default target model for that provider.
17. `test_select_models_free_text_entry()` -- Mock input returning "my-custom-model-v2" for target, "" for preproc. Verify returned model entry has `target_model="my-custom-model-v2"`. This tests DSC-03.
18. `test_select_models_list_triggers_browser()` -- Mock input sequence: "list" then "q" then "" (accept default after browser cancel) for target, "" for preproc. Patch `_browse_models` to return None. Verify `_browse_models` was called.

### Section 6: Validation ping tests (WIZ-06)

19. `test_validate_api_key_uses_model_id()` -- Patch `anthropic.Anthropic` with MagicMock. Call `validate_api_key("anthropic", "ANTHROPIC_API_KEY", model_id="claude-test-model")`. Verify `client.messages.create` was called with `model="claude-test-model"`.
20. `test_validate_api_key_falls_back_to_default_without_model_id()` -- Call without model_id. Verify `client.messages.create` was called with the hardcoded default model.
21. `test_validate_models_keeps_on_failure_if_user_says_yes()` -- Mock `validate_api_key` returning `(False, "error")`. Mock input returning "" (yes/keep). Verify model not removed from result.
22. `test_validate_models_removes_on_failure_if_user_says_no()` -- Mock `validate_api_key` returning `(False, "error")`. Mock input returning "n". Verify model removed from result.

### Section 7: Budget preview tests (WIZ-05)

23. `test_build_budget_preview_contains_pilot_and_full()` -- Call with known model. Verify output contains "Pilot run" and "Full run" and "$" amounts.
24. `test_build_budget_preview_unknown_model_shows_pricing_unknown()` -- Call with model_id not in registry. Verify "pricing unknown" in output.
25. `test_build_budget_preview_warns_over_50()` -- Create a scenario where full run exceeds $50 (may need to mock registry pricing or use expensive model). Verify "exceeds $50" in output.

### Section 8: Wizard flow integration tests (WIZ-02)

26. `test_wizard_non_interactive_creates_config()` -- Same as existing test, adapted to new code.
27. `test_wizard_ctrl_c_cancels_cleanly(capsys)` -- Mock input_fn raising KeyboardInterrupt. Verify "Setup cancelled." in output.
28. `test_wizard_interactive_full_flow(tmp_path)` -- Mock full sequence of inputs for a 2-provider setup (anthropic + google). Patch `validate_api_key` to return (True, "ok"). Patch `save_config`. Verify `save_config` called with config dict containing `models` list with 2 target entries + 2 preproc entries.

### Section 9: Existing config re-entry tests

29. `test_detect_existing_config_returns_none_when_no_file()` -- Patch `find_config_path` returning None. Verify returns None.
30. `test_handle_existing_config_reconfigure()` -- Mock input returning "2". Verify returns "reconfigure".
31. `test_handle_existing_config_default_is_reconfigure()` -- Mock input returning "". Verify returns "reconfigure".

### Section 10: Config dict building

32. `test_build_config_dict_has_models_list()` -- Call with 2 model entries. Verify result has `models` key with list containing target and preproc entries.
33. `test_build_config_dict_deduplicates_preproc()` -- Call with 2 models sharing same preproc. Verify preproc appears only once in models list.

**Test patterns:**
- Use `@patch.dict(os.environ, {...})` for env var isolation
- Use `@patch("src.setup_wizard.write_env")` to prevent actual file writes
- Use `@patch("src.setup_wizard.save_config")` to prevent actual config writes
- Use `@patch("src.setup_wizard.find_config_path")` for config detection tests
- Build input mocks as: `inputs = [...]; input_fn = lambda prompt="": inputs.pop(0)`
- Use `capsys` for stdout capture verification
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_setup_wizard.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_setup_wizard.py contains `test_mask_key_long_key`
    - tests/test_setup_wizard.py contains `test_parse_provider_selection_valid`
    - tests/test_setup_wizard.py contains `test_collect_api_keys_new_key_writes_env`
    - tests/test_setup_wizard.py contains `test_explain_model_roles_prints_explanation`
    - tests/test_setup_wizard.py contains `test_select_models_free_text_entry`
    - tests/test_setup_wizard.py contains `test_validate_api_key_uses_model_id`
    - tests/test_setup_wizard.py contains `test_build_budget_preview_contains_pilot_and_full`
    - tests/test_setup_wizard.py contains `test_wizard_non_interactive_creates_config`
    - tests/test_setup_wizard.py contains `test_wizard_ctrl_c_cancels_cleanly`
    - tests/test_setup_wizard.py contains `test_wizard_interactive_full_flow`
    - tests/test_setup_wizard.py contains `test_detect_existing_config_returns_none_when_no_file`
    - tests/test_setup_wizard.py contains `test_build_config_dict_has_models_list`
    - `grep -c "def test_" tests/test_setup_wizard.py` shows 25 or more test functions
    - `.venv/bin/python3 -m pytest tests/test_setup_wizard.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    All 30+ tests pass. Every requirement (WIZ-01 through WIZ-06, DSC-03) has at least one dedicated test. Helper functions, wizard flow, edge cases (Ctrl+C, invalid input, existing config), and integration points (env_manager, validation pings, budget preview) are all covered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and fix any regressions</name>
  <files>src/setup_wizard.py, tests/test_setup_wizard.py</files>
  <read_first>
    - src/setup_wizard.py (check for any issues surfaced by tests)
    - tests/test_setup_wizard.py (check test output for failures)
  </read_first>
  <action>
Run the full project test suite: `.venv/bin/python3 -m pytest tests/ -v --timeout=60`. Fix any failures in either `src/setup_wizard.py` or `tests/test_setup_wizard.py`. Common issues to watch for:

1. Other test files that import from `src.setup_wizard` (e.g., importing old `PROVIDERS` dict or old function signatures). Search with `grep -r "from src.setup_wizard" tests/` and `grep -r "setup_wizard" tests/` to find all references. Update any external references to use new exports.

2. If any test in `tests/test_setup_wizard.py` fails due to implementation differences from what was specified in Plan 01, fix the test to match the actual implementation (read the actual code, don't assume).

3. If the full suite reveals import issues or circular dependencies in `setup_wizard.py`, fix the source.

Ensure the full suite passes cleanly.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/ -v --timeout=60 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3 -m pytest tests/ --timeout=60` exits 0
    - No test files reference old `PROVIDERS` dict from setup_wizard unless it still exists as compatibility export
    - `grep -c "FAILED" <<< "$(.venv/bin/python3 -m pytest tests/ -v --timeout=60 2>&1)"` returns 0
  </acceptance_criteria>
  <done>
    Full test suite passes with zero failures. No regressions introduced by the wizard rewrite. All existing tests in other modules continue to pass.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python3 -m pytest tests/test_setup_wizard.py -x -q` exits 0 (wizard tests pass)
- `.venv/bin/python3 -m pytest tests/ --timeout=60` exits 0 (full suite passes)
- `grep -c "def test_" tests/test_setup_wizard.py` shows 25+ (comprehensive coverage)
</verification>

<success_criteria>
tests/test_setup_wizard.py has 25+ tests covering all wizard functions and flows. All tests pass. The full project test suite passes with zero regressions. Every phase requirement has at least one dedicated test.
</success_criteria>

<output>
After completion, create `.planning/phases/19-setup-wizard-overhaul/19-02-SUMMARY.md`
</output>
