---
phase: 20-update-skills-and-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/validate-rdd/SKILL.md
  - .claude/skills/run-experiment/SKILL.md
  - .claude/skills/run-pilot/SKILL.md
  - .claude/skills/check-results/SKILL.md
  - .claude/skills/check-results/references/db-context.md
  - .claude/skills/analyze/SKILL.md
  - .claude/skills/generate-figures/SKILL.md
  - .claude/skills/write-section/SKILL.md
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03, SKL-04, SKL-05, SKL-06, SKL-07]

must_haves:
  truths:
    - "Every SKILL.md references model_registry instead of hardcoded MODELS/PRICE_TABLE/PREPROC_MODEL_MAP/RATE_LIMIT_DELAYS"
    - "Every SKILL.md references config_manager for config loading instead of flat config fields"
    - "No SKILL.md contains the string 'config.py:MODELS' or 'config.py:PRICE_TABLE' or 'config.py:RATE_LIMIT_DELAYS' or 'config.py:PREPROC_MODEL_MAP' or 'config.py:compute_cost'"
    - "check-results db-context.md price table notes that pricing comes from model registry, not hardcoded values"
    - "Skills that mention setup reference the rewritten multi-provider wizard and propt list-models"
  artifacts:
    - path: ".claude/skills/validate-rdd/SKILL.md"
      provides: "Updated RDD validation skill with v2.0 module references"
      contains: "model_registry"
    - path: ".claude/skills/run-experiment/SKILL.md"
      provides: "Updated experiment execution skill"
      contains: "registry"
    - path: ".claude/skills/run-pilot/SKILL.md"
      provides: "Updated pilot skill"
      contains: "env_manager"
    - path: ".claude/skills/check-results/SKILL.md"
      provides: "Updated results checking skill"
      contains: "model_registry"
    - path: ".claude/skills/check-results/references/db-context.md"
      provides: "Updated database context with registry-backed pricing note"
      contains: "model_registry"
    - path: ".claude/skills/analyze/SKILL.md"
      provides: "Updated analysis skill acknowledging dynamic models"
      contains: "configured models"
    - path: ".claude/skills/generate-figures/SKILL.md"
      provides: "Updated figures skill acknowledging variable model counts"
      contains: "configured models"
    - path: ".claude/skills/write-section/SKILL.md"
      provides: "Updated paper writing skill"
      contains: "configurable"
  key_links:
    - from: ".claude/skills/validate-rdd/SKILL.md"
      to: "src/model_registry.py"
      via: "module reference in validation dimensions"
      pattern: "model_registry"
    - from: ".claude/skills/run-experiment/SKILL.md"
      to: "src/model_registry.py"
      via: "troubleshooting table references"
      pattern: "registry"
---

<objective>
Update all 7 SKILL.md files and the check-results db-context.md reference file to replace stale v1.0 references with correct v2.0 module paths and APIs.

Purpose: Skills currently reference removed constants (MODELS, PRICE_TABLE, PREPROC_MODEL_MAP, RATE_LIMIT_DELAYS, compute_cost from config.py) that no longer exist. This causes skills to give incorrect guidance when triggered.

Output: All 8 files updated with correct v2.0 references. No stale config.py constant references remain.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-update-skills-and-agents-in-claude-using-the-skill-creator-skill-and-re-run-all-optimizations-and-evaluations/20-CONTEXT.md
@.planning/phases/20-update-skills-and-agents-in-claude-using-the-skill-creator-skill-and-re-run-all-optimizations-and-evaluations/20-RESEARCH.md

<interfaces>
<!-- V2.0 module reference table — the executor MUST use these replacements -->

| Old Reference (REMOVE from skills) | New Reference (ADD to skills) | Module |
|-------------------------------------|-------------------------------|--------|
| `config.py:MODELS` | `registry.target_models()` | `src/model_registry.py` |
| `config.py:PRICE_TABLE` | `registry.get_price(model_id)` | `src/model_registry.py` |
| `config.py:PREPROC_MODEL_MAP` | `registry.get_preproc(model_id)` | `src/model_registry.py` |
| `config.py:RATE_LIMIT_DELAYS` | `registry.get_delay(model_id)` | `src/model_registry.py` |
| `config.py:compute_cost()` | `registry.compute_cost(model_id, in_tok, out_tok)` | `src/model_registry.py` |
| flat config fields (`claude_model`, etc.) | `ExperimentConfig.models` list | `src/config.py` + `src/config_manager.py` |
| manual .env editing | `env_manager.write_env()` / `env_manager.check_keys()` | `src/env_manager.py` |
| N/A (new) | `discover_all_models()` | `src/model_discovery.py` |
| old setup wizard | `run_setup_wizard()` (16 functions, multi-provider) | `src/setup_wizard.py` |
| N/A (new) | `propt list-models` CLI command | `src/cli.py` |

What REMAINS in config.py (unchanged — do NOT remove these references):
- `ExperimentConfig` dataclass (with `models` list field, v2 format)
- `derive_seed()` function
- `NOISE_TYPES` tuple
- `INTERVENTIONS` tuple
- `OPENROUTER_BASE_URL`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HIGH-impact skills (validate-rdd, run-experiment, run-pilot, check-results + db-context.md)</name>
  <files>
    .claude/skills/validate-rdd/SKILL.md
    .claude/skills/run-experiment/SKILL.md
    .claude/skills/run-pilot/SKILL.md
    .claude/skills/check-results/SKILL.md
    .claude/skills/check-results/references/db-context.md
  </files>
  <read_first>
    .claude/skills/validate-rdd/SKILL.md
    .claude/skills/run-experiment/SKILL.md
    .claude/skills/run-pilot/SKILL.md
    .claude/skills/check-results/SKILL.md
    .claude/skills/check-results/references/db-context.md
    src/model_registry.py
    src/config_manager.py
    src/env_manager.py
    src/model_discovery.py
    src/setup_wizard.py
  </read_first>
  <action>
Update each of the 4 HIGH-impact SKILL.md files using the stale reference audit from 20-RESEARCH.md. Apply ALL of the following replacements. Preserve the existing YAML frontmatter (name + description fields) — trigger descriptions will be re-optimized in Plan 02.

**validate-rdd/SKILL.md changes:**
1. Section "3. Model configuration (RDD Section 4.2)" — replace the table:
   - Row "Models under test": change `config.py:MODELS` to `model_registry.target_models()` (from `src/model_registry.py`). Change description from "Claude Sonnet + Gemini 1.5 Pro (2 models)" to "Dynamically configured models from model registry (1-4+ models)"
   - Row "Model versions pinned": change `config.py model string constants` to `model_registry model configs (ModelConfig.model_id)`
   - Row "Pre-processor models": change `config.py:PREPROC_MODEL_MAP` to `model_registry.get_preproc(model_id)` (from `src/model_registry.py`)
2. Section "7. Logging and instrumentation" — row "Cost tracking": change `config.py:compute_cost()` to `model_registry.compute_cost(model_id, in_tok, out_tok)` (from `src/model_registry.py`)
3. Add a NEW validation dimension section "8. Configuration management" that checks:
   - `config_manager.py:load_config()` loads ExperimentConfig v2 with `models` list field
   - `config_manager.py:save_config()` persists to JSON
   - `config_manager.py:validate_config()` validates the config
   - `model_registry.py:ModelRegistry` singleton is the source of truth for model data
   - `env_manager.py:check_keys()` validates API keys are present
   - `setup_wizard.py:run_setup_wizard()` configures models and providers
   - `model_discovery.py:discover_all_models()` queries live model listings

**run-experiment/SKILL.md changes:**
1. Common issues table — row "Rate limit errors": change `config.py:RATE_LIMIT_DELAYS` to `model_registry.get_delay(model_id)` (from `src/model_registry.py`)
2. Prerequisites section — after item 3 "API keys set", add: "Verify with `env_manager.check_keys()` or `propt validate`"
3. Add to "Important notes" section: "Model resolution uses `config_manager.load_config()` and `model_registry` — models are dynamically configured, not hardcoded"
4. Add to "Important notes": "Cost estimation uses `model_registry.compute_cost()` with registry-backed pricing"

**run-pilot/SKILL.md changes:**
1. Prerequisites section — item 1 "API keys are set": add mention of `env_manager.check_keys()` as the programmatic way to verify keys
2. Prerequisites section — item 2 "Config exists": mention `config_manager.load_config()` as the config loading mechanism
3. Common issues table — row "API rate limits": change `config.py:RATE_LIMIT_DELAYS` to `model_registry.get_delay(model_id)` (from `src/model_registry.py`)
4. Add to "Important notes": "The pilot uses dynamically configured models from the model registry — run `propt setup` to configure providers and models before piloting"

**check-results/SKILL.md changes:**
1. The skill already uses `config_manager.find_config_path` (partially updated). Verify this is correct and add: after the config loading block, add a comment noting that model pricing comes from `model_registry.get_price(model_id)`, not a hardcoded table
2. In "Important notes" section, add: "Model pricing is dynamically loaded from model_registry — the hardcoded price table in db-context.md is approximate; use registry.compute_cost() for precise costs"
3. In recommendations, where it suggests `propt run`, also mention `propt list-models` for discovering available models

**check-results/references/db-context.md changes:**
1. In the "Price table" section, add a NOTE above the table: "NOTE: These are approximate default prices. Actual pricing is loaded dynamically from the model registry via `model_registry.get_price(model_id)`. For OpenRouter models, live pricing is fetched from the OpenRouter API."
2. In the "Models" line under "Experimental design constants", change from "Up to 4 (claude, gemini, gpt-4o, openrouter/nemotron)" to "Dynamically configured via model registry (default: claude, gemini, gpt-4o, openrouter/nemotron)"
3. In the "CLI commands" section, add: `propt list-models` — list available models from configured providers
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && grep -c "config\.py:MODELS\|config\.py:PRICE_TABLE\|config\.py:RATE_LIMIT_DELAYS\|config\.py:PREPROC_MODEL_MAP\|config\.py:compute_cost" .claude/skills/validate-rdd/SKILL.md .claude/skills/run-experiment/SKILL.md .claude/skills/run-pilot/SKILL.md .claude/skills/check-results/SKILL.md .claude/skills/check-results/references/db-context.md 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep for "config.py:MODELS" across all 5 files returns 0 matches
    - grep for "config.py:RATE_LIMIT_DELAYS" across all 5 files returns 0 matches
    - grep for "config.py:PREPROC_MODEL_MAP" across all 5 files returns 0 matches
    - grep for "config.py:compute_cost" across all 5 files returns 0 matches
    - validate-rdd/SKILL.md contains "model_registry" (at least 3 occurrences)
    - validate-rdd/SKILL.md contains "Configuration management" section header
    - validate-rdd/SKILL.md contains "config_manager" and "env_manager" and "setup_wizard" and "model_discovery"
    - run-experiment/SKILL.md contains "model_registry.get_delay"
    - run-experiment/SKILL.md contains "model_registry.compute_cost"
    - run-pilot/SKILL.md contains "env_manager.check_keys"
    - run-pilot/SKILL.md contains "model_registry.get_delay"
    - check-results/references/db-context.md contains "model_registry.get_price"
    - check-results/references/db-context.md contains "propt list-models"
    - The automated verify command outputs "0" (zero files with stale references)
  </acceptance_criteria>
  <done>All 4 HIGH-impact SKILL.md files and db-context.md updated with v2.0 module references. Zero stale config.py constant references remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Update LOW-impact skills (analyze, generate-figures, write-section)</name>
  <files>
    .claude/skills/analyze/SKILL.md
    .claude/skills/generate-figures/SKILL.md
    .claude/skills/write-section/SKILL.md
  </files>
  <read_first>
    .claude/skills/analyze/SKILL.md
    .claude/skills/generate-figures/SKILL.md
    .claude/skills/write-section/SKILL.md
  </read_first>
  <action>
Update the 3 LOW-impact SKILL.md files with minor wording changes to acknowledge v2.0 dynamic model configuration. Preserve the existing YAML frontmatter (name + description fields) unchanged.

**analyze/SKILL.md changes:**
1. In "Pipeline overview" section, after the 3-stage pipeline diagram, add a note: "Note: The analysis pipeline processes results for all models configured in the model registry. The number and identity of models is determined at setup time via `propt setup`, not hardcoded."
2. In "Important notes" at the bottom, add: "Results include data for all configured models — the analysis automatically handles variable model counts"

**generate-figures/SKILL.md changes:**
1. In "Reviewing figures" section, item 3 "Completeness", change from "Are all models and conditions represented?" to "Are all configured models and conditions represented? (Model count varies based on registry configuration)"
2. In "Prerequisites" section, add: "Model set is determined by the configuration — figures automatically adapt to however many models are in the results"

**write-section/SKILL.md changes:**
1. In section "4. Section-specific guidance", under "Methodology", change from "Reference the exact model versions and seeds" to "Reference the exact model versions (from model registry configuration) and seeds. Note that models are configurable — the methodology should describe this as a feature of the toolkit."
2. In "Important notes" at the bottom, add: "The toolkit supports configurable models — methodology and setup sections should reflect this. Model versions come from model_registry, not hardcoded constants."
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && grep -l "configured models\|model registry\|configurable" .claude/skills/analyze/SKILL.md .claude/skills/generate-figures/SKILL.md .claude/skills/write-section/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - analyze/SKILL.md contains "model registry" or "configured models"
    - generate-figures/SKILL.md contains "configured models" or "registry configuration"
    - write-section/SKILL.md contains "model registry" or "configurable models"
    - The automated verify command outputs "3" (all 3 files contain the new references)
    - No stale references to "config.py:MODELS" or "config.py:PRICE_TABLE" exist (these files had none to begin with, but verify)
  </acceptance_criteria>
  <done>All 3 LOW-impact SKILL.md files updated with dynamic model acknowledgments. All 7 skills now reflect v2.0 module architecture.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a comprehensive stale reference check:

```bash
# Zero stale references across ALL skill files
grep -r "config\.py:MODELS\|config\.py:PRICE_TABLE\|config\.py:RATE_LIMIT_DELAYS\|config\.py:PREPROC_MODEL_MAP\|config\.py:compute_cost" .claude/skills/ | grep -v "\.json" | wc -l
# Expected: 0

# All HIGH-impact skills reference model_registry
grep -l "model_registry" .claude/skills/validate-rdd/SKILL.md .claude/skills/run-experiment/SKILL.md .claude/skills/run-pilot/SKILL.md .claude/skills/check-results/SKILL.md | wc -l
# Expected: 4

# All LOW-impact skills reference dynamic models
grep -l "configured models\|model registry\|configurable" .claude/skills/analyze/SKILL.md .claude/skills/generate-figures/SKILL.md .claude/skills/write-section/SKILL.md | wc -l
# Expected: 3
```
</verification>

<success_criteria>
- All 7 SKILL.md files updated with v2.0 references
- db-context.md updated with registry pricing note and propt list-models
- Zero stale config.py constant references remain in any skill file
- validate-rdd has new "Configuration management" validation dimension
- All existing YAML frontmatter preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/20-update-skills-and-agents-in-claude-using-the-skill-creator-skill-and-re-run-all-optimizations-and-evaluations/20-01-SUMMARY.md`
</output>
