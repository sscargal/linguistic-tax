---
phase: 21-update-all-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-07

must_haves:
  truths:
    - "README.md Quick Start shows clone/sync/setup workflow with wizard handling keys and config"
    - "README.md models table is labeled as defaults with configurable note"
    - "README.md project structure lists 21 modules, 25 test files, includes 3 new modules and default_models.json"
    - "CLAUDE.md architecture section lists all 21 modules including model_registry, env_manager, model_discovery"
    - "No stale references to PRICE_TABLE, PREPROC_MODEL_MAP, RATE_LIMIT_DELAYS, or Python 3.11 in either file"
  artifacts:
    - path: "README.md"
      provides: "Updated root readme reflecting v2.0 architecture"
      contains: "Default models (configurable via"
    - path: "CLAUDE.md"
      provides: "Updated project instructions reflecting v2.0 modules"
      contains: "model_registry.py"
  key_links:
    - from: "README.md"
      to: "docs/getting-started.md"
      via: "relative link"
      pattern: "docs/getting-started.md"
    - from: "README.md"
      to: "docs/architecture.md"
      via: "relative link"
      pattern: "docs/architecture.md"
---

<objective>
Update README.md and CLAUDE.md to reflect v2.0 architecture: configurable models via ModelRegistry, .env API key management, simplified Quick Start, updated project structure, corrected module counts, and stale reference removal.

Purpose: These two root-level docs define project identity for new users (README.md) and Claude Code (CLAUDE.md). Both share overlapping content (project structure, tech stack) that must be consistent.
Output: Two updated Markdown files with all v1.0 references replaced by v2.0 equivalents.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-update-all-documentation/21-CONTEXT.md
@.planning/phases/21-update-all-documentation/21-RESEARCH.md

<interfaces>
<!-- Source of truth files the executor must read for accurate v2.0 content -->

From src/model_registry.py:
```python
@dataclass
class ModelConfig:
    model_id: str
    provider: str
    role: str
    preproc_model_id: str | None = None
    input_price_per_1m: float | None = None
    output_price_per_1m: float | None = None
    rate_limit_delay: float | None = None
```

From src/config.py (v2.0 ExperimentConfig):
```python
@dataclass
class ExperimentConfig:
    models: list[dict] | None = None
    config_version: int = 2
    base_seed: int = 42
    type_a_rates: tuple[float, ...] = (0.05, 0.10, 0.20)
    type_a_weights: tuple[float, ...] = (0.40, 0.25, 0.20, 0.15)
    repetitions: int = 5
    temperature: float = 0.0
    prompts_path: str = "data/prompts.json"
    matrix_path: str = "data/experiment_matrix.json"
    results_db_path: str = "results/results.db"
```

From src/cli.py (list-models flags):
```python
models_parser = subparsers.add_parser("list-models", help="List available models with pricing")
models_parser.add_argument("--json", action="store_true", default=False, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md for v2.0</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state)
    - src/model_registry.py (ModelConfig, ModelRegistry API)
    - src/config.py (ExperimentConfig v2.0 fields)
    - src/cli.py (CLI subcommands and flags, especially list-models --json)
    - src/config_commands.py (valid set-config properties for v2.0 examples)
    - data/default_models.json (default model configurations)
    - .planning/phases/21-update-all-documentation/21-CONTEXT.md (locked decisions)
    - .planning/phases/21-update-all-documentation/21-RESEARCH.md (stale patterns and replacements)
  </read_first>
  <action>
Update README.md with these specific changes (reference RESEARCH.md Section "1. README.md" for line numbers):

**Python version:** Find all occurrences of `Python >= 3.11` or `Python 3.11` and change to `Python >= 3.12` or `Python 3.12+`.

**Quick Start section:** Simplify to 3 steps:
1. `git clone` the repo
2. `uv sync` to install dependencies
3. `propt setup` to run the wizard (wizard handles API keys, model config, everything)

**API Keys section:** Restructure to lead with .env:
- Primary: `.env` file at project root (created by `propt setup` or manually)
- Alternative: `export` commands in shell
- Show .env file format with ANTHROPIC_API_KEY, GOOGLE_API_KEY, OPENAI_API_KEY, OPENROUTER_API_KEY

**Configuration section:** Rewrite wizard description for v2.0 multi-provider flow:
- Multi-provider loop (configure 1-4 providers per session)
- Free-text model entry with defaults
- .env file creation for API keys
- Validation pings for each model
- Budget preview before committing
- Emphasize `propt setup` as primary config path
- Minimal mention of `propt set-config` with link to CLI reference

**CLI reference table:** Update `list-models` row to include `--json` flag:
`| propt list-models | List available models with pricing | --json |`

**set-config examples:** Read src/config_commands.py handle_set_config() to determine valid v2.0 properties. Replace any examples using `claude_model`, `gemini_model`, etc. with valid v2.0 property names (likely `base_seed`, `repetitions`, `temperature`, etc.).

**Models and Pricing table:** Add label "Default models (configurable via `propt setup`)" above the table. Keep the 4 default models but make clear they are defaults.

**Experiment Design diagram:** Change "4 Target Models" to "N Target Models (configurable)".

**Work item count:** Present as formula: `prompts x noise_types x interventions x models x repetitions` with default example showing ~82K with 4 models.

**Project Structure tree:**
- Change `src/` comment to `# 21 Python modules`
- Change `tests/` comment to `# 25 test files`
- Add these 3 modules in alphabetical position under src/:
  ```
  env_manager.py                  # .env file loading, writing, and API key management
  model_discovery.py              # Live model queries from provider APIs
  model_registry.py               # Config-driven pricing, preproc mappings, rate limits
  ```
- Add under data/:
  ```
  default_models.json             # Default model configurations for 4 providers
  ```

**Glossary:** Update entries that reference `MODELS in src/config.py` or `PREPROC_MODEL_MAP in src/config.py` to reference `ModelRegistry` in `src/model_registry.py`.

**Bibtex citation:** Update with author name placeholder (e.g., `author = {Author Name}`) and arxiv URL placeholder (e.g., `url = {https://arxiv.org/abs/XXXX.XXXXX}`).

**Systematic stale reference sweep within README.md:**
- grep for PRICE_TABLE, PREPROC_MODEL_MAP, RATE_LIMIT_DELAYS -- replace with ModelRegistry references
- grep for `18 Python modules` -- change to `21`
- grep for `19 test files` -- change to `25`
- grep for `config.py` used as source of model data -- update to `model_registry.py` / `data/default_models.json` (keep references to config.py for ExperimentConfig, NOISE_TYPES, INTERVENTIONS)

**Silent update:** Do NOT add any "What's New in v2.0" section. Just update everything in place.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && grep -cn "PRICE_TABLE\|PREPROC_MODEL_MAP\|RATE_LIMIT_DELAYS" README.md && grep -cn "3\.11" README.md && grep -cn "18 Python modules" README.md && grep -cn "19 test files" README.md && grep -c "Default models (configurable" README.md && grep -c "model_registry.py" README.md && grep -c "default_models.json" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains zero occurrences of `PRICE_TABLE`, `PREPROC_MODEL_MAP`, `RATE_LIMIT_DELAYS` as config constants
    - README.md contains zero occurrences of `3.11` (Python version)
    - README.md contains zero occurrences of `18 Python modules`
    - README.md contains zero occurrences of `19 test files`
    - README.md contains `Default models (configurable via`
    - README.md contains `model_registry.py`
    - README.md contains `default_models.json`
    - README.md contains `env_manager.py`
    - README.md contains `model_discovery.py`
    - README.md contains `21 Python modules`
    - README.md contains `25 test files`
    - README.md contains `--json` in the list-models CLI row
    - README.md contains `propt setup` in Quick Start section
    - README.md contains `N Target Models` or `N Target Models (configurable)`
    - README.md does NOT contain `claude_model` or `gemini_model` as config field examples
    - README.md does NOT contain a "What's New" section
  </acceptance_criteria>
  <done>README.md fully reflects v2.0 architecture with no stale v1.0 references to model constants, correct module/test counts, simplified Quick Start, .env-first API keys, and updated CLI reference</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md for v2.0</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state)
    - src/model_registry.py (module purpose)
    - src/env_manager.py (module purpose)
    - src/model_discovery.py (module purpose)
    - data/default_models.json (file purpose)
    - .planning/phases/21-update-all-documentation/21-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Update CLAUDE.md with these specific changes:

**Architecture tree:** Add these 3 modules in alphabetical position under src/:
```
  env_manager.py       -- .env file loading, writing, and API key management
  model_discovery.py   -- Live model queries from provider APIs
  model_registry.py    -- Config-driven pricing, preproc mappings, rate limits
```

Add under data/:
```
  default_models.json  -- Default model configurations for 4 providers
```

**Tech Stack section:** Update APIs line from:
`- **APIs:** Anthropic (Claude), Google (Gemini) -- direct API calls, NOT CLI`
to:
`- **APIs:** Anthropic (Claude), Google (Gemini), OpenAI (GPT), OpenRouter -- direct API calls, NOT CLI`

**Important Context section:** Update the line about pre-processor calls:
From: `- Pre-processor calls use a CHEAP model (Haiku or Flash)`
To: `- Pre-processor calls use a CHEAP model (configurable per provider via ModelRegistry)`

**Stale reference sweep:** Check for any remaining references to hardcoded MODELS, PRICE_TABLE, PREPROC_MODEL_MAP, RATE_LIMIT_DELAYS as config.py constants and update them. Keep references to config.py for ExperimentConfig, NOISE_TYPES, INTERVENTIONS (these are still in config.py).

**Python version:** Verify it already says `Python 3.12+` (it does per current reading). No change needed if correct.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && grep -c "model_registry.py" CLAUDE.md && grep -c "env_manager.py" CLAUDE.md && grep -c "model_discovery.py" CLAUDE.md && grep -c "default_models.json" CLAUDE.md && grep -c "OpenRouter" CLAUDE.md && grep -cn "PRICE_TABLE\|PREPROC_MODEL_MAP\|RATE_LIMIT_DELAYS" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `model_registry.py`
    - CLAUDE.md contains `env_manager.py`
    - CLAUDE.md contains `model_discovery.py`
    - CLAUDE.md contains `default_models.json`
    - CLAUDE.md contains `OpenRouter` in Tech Stack
    - CLAUDE.md contains `OpenAI` in Tech Stack
    - CLAUDE.md contains zero occurrences of `PRICE_TABLE`, `PREPROC_MODEL_MAP`, `RATE_LIMIT_DELAYS`
    - CLAUDE.md contains `configurable per provider` or similar for pre-processor model reference
  </acceptance_criteria>
  <done>CLAUDE.md lists all 4 providers in Tech Stack, includes 3 new modules and default_models.json in architecture tree, and has no stale v1.0 constant references</done>
</task>

</tasks>

<verification>
Run stale pattern grep across both files:
```bash
grep -rn "PRICE_TABLE\|PREPROC_MODEL_MAP\|RATE_LIMIT_DELAYS\|3\.11\|18 Python modules\|19 test files" README.md CLAUDE.md
```
Expected: zero results.

Verify new content present:
```bash
grep -c "model_registry.py" README.md CLAUDE.md
grep -c "default_models.json" README.md CLAUDE.md
```
Expected: at least 1 match in each file.

Ensure no code was accidentally changed:
```bash
pytest tests/ -x -q
```
</verification>

<success_criteria>
- README.md and CLAUDE.md contain zero stale v1.0 references to model constants
- Both files reference all 3 new v2.0 modules and default_models.json
- README.md has simplified Quick Start, .env-first API keys, updated CLI reference, formula-based work item count
- CLAUDE.md lists all 4 providers in Tech Stack
- pytest still passes (no accidental code changes)
</success_criteria>

<output>
After completion, create `.planning/phases/21-update-all-documentation/21-01-SUMMARY.md`
</output>
