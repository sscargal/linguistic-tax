---
phase: 21-update-all-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started.md
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "Getting-started guide shows .env as primary API key method with export as alternative"
    - "Wizard section describes multi-provider flow with .env creation, validation pings, and budget preview"
    - "Python version is 3.12+ throughout the document"
    - "No stale references to hardcoded model constants or v1.0 wizard flow"
  artifacts:
    - path: "docs/getting-started.md"
      provides: "Updated setup walkthrough reflecting v2.0 wizard and config flow"
      contains: "propt setup"
  key_links:
    - from: "docs/getting-started.md"
      to: "README.md"
      via: "relative link"
      pattern: "../README.md"
---

<objective>
Rewrite docs/getting-started.md to reflect the v2.0 setup wizard flow: multi-provider configuration, .env-first API key management, validation pings, budget preview, and configurable models.

Purpose: This is the primary onboarding document for new users. The wizard was completely overhauled in Phase 19 and the current docs still describe the v1.0 single-provider flow.
Output: Updated getting-started.md with v2.0 wizard walkthrough and .env-first key management.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-update-all-documentation/21-CONTEXT.md
@.planning/phases/21-update-all-documentation/21-RESEARCH.md

<interfaces>
<!-- Source of truth for v2.0 wizard flow -->

From src/setup_wizard.py (key functions defining the flow):
- run_wizard() - Main entry: multi-provider loop
- _select_providers() - Choose 1-4 providers
- _configure_provider_models() - Free-text model entry with defaults per provider
- _handle_api_keys() - .env file creation/update
- _validate_models() - Ping each model to verify access
- _show_budget_preview() - Cost estimate before committing

From src/config.py (v2.0 ExperimentConfig):
```python
@dataclass
class ExperimentConfig:
    models: list[dict] | None = None
    config_version: int = 2
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite getting-started.md for v2.0</name>
  <files>docs/getting-started.md</files>
  <read_first>
    - docs/getting-started.md (current state -- essential to see existing structure)
    - src/setup_wizard.py (actual v2.0 wizard flow -- source of truth for wizard description)
    - src/env_manager.py (how .env files work)
    - src/execution_summary.py (format_summary function -- source of truth for pilot --dry-run output format)
    - src/config_commands.py (what set-config accepts in v2.0)
    - data/default_models.json (default model names per provider)
    - .planning/phases/21-update-all-documentation/21-CONTEXT.md (locked decisions)
    - .planning/phases/21-update-all-documentation/21-RESEARCH.md (stale patterns list)
  </read_first>
  <action>
Update docs/getting-started.md with these specific changes:

**Python version:** Change all occurrences of `Python >= 3.11` or `3.11` to `Python >= 3.12` or `3.12+`.

**Environment Variables / API Keys section:** Restructure to present two equal options:

Option 1 -- .env file (recommended, created by `propt setup`):
```
# .env (project root)
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=AI...
OPENAI_API_KEY=sk-...
OPENROUTER_API_KEY=sk-or-...
```

Option 2 -- Shell export:
```bash
export ANTHROPIC_API_KEY="sk-ant-..."
export GOOGLE_API_KEY="AI..."
```

Note: `propt setup` creates the .env file automatically when you provide keys.

**Wizard Walkthrough section:** Rewrite entirely to describe v2.0 multi-provider flow. Read src/setup_wizard.py to understand the actual sequence. Key points to cover:
1. Provider selection -- choose which providers to configure (1-4)
2. For each provider: enter API key (stored in .env), enter target model ID (free text with default shown), enter pre-processor model (free text with default)
3. Validation -- wizard pings each model with a tiny request to verify access
4. Budget preview -- estimated experiment cost displayed based on selected models' pricing
5. Confirmation -- user reviews and commits configuration

Keep existing walkthrough scenarios but update their content to reflect v2.0 flow. Do NOT add new scenarios per CONTEXT.md decision.

**Configuration section:** Wizard-first approach:
- Lead with `propt setup` as primary configuration path
- Minimal manual config section showing `propt set-config` basics
- Read src/config_commands.py to determine what properties are settable in v2.0 and use those in examples (NOT `claude_model`, `gemini_model`, etc.)
- Mention budget preview feature

**Provider/model table:** If there is a table of hardcoded model names per provider, label them as defaults (configurable via wizard).

**Sample output:** Where the doc shows sample terminal output for pilot runs or other commands:
- Read src/execution_summary.py format_summary() to understand v2.0 output format
- Update sample output to reflect v2.0 format (show configurable model names, updated formatting)
- If exact output is hard to determine, write plausible v2.0 output based on the format_summary() code

**Stale reference sweep:**
- grep for PRICE_TABLE, PREPROC_MODEL_MAP, RATE_LIMIT_DELAYS -- replace with ModelRegistry references
- grep for `config.py` used as source of model data -- update appropriately
- grep for `claude_model`, `gemini_model` as config fields -- update to v2.0 models list pattern

**Structure:** Allow restructuring where current structure is misleading for v2.0 (e.g., if wizard section is buried after manual config, move it up). Otherwise update in place.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && grep -cn "PRICE_TABLE\|PREPROC_MODEL_MAP\|RATE_LIMIT_DELAYS" docs/getting-started.md && grep -cn "3\.11" docs/getting-started.md && grep -c "propt setup" docs/getting-started.md && grep -c "\.env" docs/getting-started.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started.md contains zero occurrences of `PRICE_TABLE`, `PREPROC_MODEL_MAP`, `RATE_LIMIT_DELAYS`
    - docs/getting-started.md contains zero occurrences of `3.11` (Python version)
    - docs/getting-started.md contains `propt setup` at least 3 times (Quick Start, wizard section, config section)
    - docs/getting-started.md contains `.env` at least 2 times (key management section)
    - docs/getting-started.md contains `budget preview` or `Budget Preview` or `cost estimate`
    - docs/getting-started.md contains `validation` or `validates` or `pings` (model validation feature)
    - docs/getting-started.md does NOT contain `claude_model` or `gemini_model` as config field names (search for them as assignment targets like `claude_model "`)
    - docs/getting-started.md contains `3.12` for Python version
  </acceptance_criteria>
  <done>Getting-started guide describes v2.0 wizard flow (multi-provider, .env, validation, budget preview), presents .env as primary key method, has no stale v1.0 references, and Python version is 3.12+</done>
</task>

</tasks>

<verification>
Stale pattern check:
```bash
grep -rn "PRICE_TABLE\|PREPROC_MODEL_MAP\|RATE_LIMIT_DELAYS\|3\.11\|claude_model\|gemini_model" docs/getting-started.md
```
Expected: zero results.

Key v2.0 content present:
```bash
grep -c "propt setup" docs/getting-started.md
grep -c "\.env" docs/getting-started.md
grep -c "budget" docs/getting-started.md
```
Expected: multiple matches each.

No code changes:
```bash
pytest tests/ -x -q
```
</verification>

<success_criteria>
- Getting-started.md wizard section describes the v2.0 multi-provider flow
- .env presented as primary API key method
- No stale v1.0 constant references
- Python version is 3.12+ throughout
- Existing walkthrough scenarios updated (not removed or added)
</success_criteria>

<output>
After completion, create `.planning/phases/21-update-all-documentation/21-02-SUMMARY.md`
</output>
