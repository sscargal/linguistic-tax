---
phase: 23-fix-pre-processor-output-quality-and-performance
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - docs/getting-started.md
  - src/setup_wizard.py
autonomous: false
requirements:
  - TODO-preproc-performance-anomaly

must_haves:
  truths:
    - "Documentation warns against using reasoning models as pre-processors"
    - "Recommended non-reasoning alternatives are listed in docs and wizard output"
    - "User can verify the fix improved accuracy by re-running type_a pilot conditions"
  artifacts:
    - path: "docs/getting-started.md"
      provides: "Pre-processor model guidance for researchers"
      contains: "non-reasoning"
    - path: "src/setup_wizard.py"
      provides: "Wizard output warns about reasoning model preproc choice"
      contains: "non-reasoning"
  key_links:
    - from: "docs/getting-started.md"
      to: "Pre-processor model table"
      via: "recommendation text in model selection section"
      pattern: "gpt-4o-mini.*claude-haiku.*gemini-2.0-flash"
---

<objective>
Add model guidance documentation and provide verification instructions for the pre-processor fixes.

Purpose: Ensure researchers know which models to use as pre-processors (non-reasoning models like gpt-4o-mini, claude-haiku, gemini-2.0-flash) and can verify the accuracy improvement from Plan 01's code changes by re-running type_a pilot conditions.

Output: Updated docs/getting-started.md with pre-processor model guidance, updated wizard print output with recommendation, and human-verified re-pilot results.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-fix-pre-processor-output-quality-and-performance/23-CONTEXT.md
@.planning/phases/23-fix-pre-processor-output-quality-and-performance/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-processor model guidance to docs and wizard</name>
  <files>docs/getting-started.md, src/setup_wizard.py</files>
  <read_first>
    - docs/getting-started.md
    - src/setup_wizard.py
  </read_first>
  <action>
1. In `docs/getting-started.md`, find the model selection section (around line 86 where the provider/model table is). Add a new subsection or note AFTER the model table:

```markdown
> **Pre-processor model choice matters.** Reasoning models (e.g., `gpt-5-nano`, `o3-mini`, `o4-mini`) generate hidden chain-of-thought that inflates output tokens (5x ratio observed) and increases latency (3.6s TTFT). Use non-reasoning models for pre-processing:
>
> | Provider | Recommended Pre-processor |
> |----------|--------------------------|
> | Anthropic | `claude-haiku-4-5-20250514` |
> | Google | `gemini-2.0-flash` |
> | OpenAI | `gpt-4o-mini` |
> | OpenRouter | Any non-reasoning model |
>
> The toolkit automatically skips pre-processing for clean and ESL prompts (where it provides no benefit), only running it for typo-injected (type_a) noise conditions.
```

2. In `src/setup_wizard.py`, find the `explain_model_roles()` function (the one that prints the explanation of target vs pre-processor models, around line 317). After the existing explanation of pre-processor models, add a print statement:

```python
print(
    "\nIMPORTANT: Use non-reasoning models for pre-processing. Reasoning models\n"
    "(gpt-5-nano, o3-mini, o4-mini) produce bloated output and high latency.\n"
    "Recommended: gpt-4o-mini, claude-haiku, gemini-2.0-flash.\n"
)
```

Place this AFTER the line that says "Pre-processor models are cheap, fast models" and BEFORE the question about preproc scope.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && grep -c "non-reasoning" docs/getting-started.md && grep -c "non-reasoning" src/setup_wizard.py && pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started.md contains "non-reasoning" at least once
    - docs/getting-started.md contains "gpt-4o-mini" in the pre-processor recommendation
    - docs/getting-started.md contains "claude-haiku" in the pre-processor recommendation
    - docs/getting-started.md contains "gemini-2.0-flash" in the pre-processor recommendation
    - docs/getting-started.md contains "skips pre-processing for clean and ESL"
    - src/setup_wizard.py contains "non-reasoning models for pre-processing"
    - src/setup_wizard.py contains "gpt-4o-mini, claude-haiku, gemini-2.0-flash"
    - pytest tests/ exits 0
  </acceptance_criteria>
  <done>Documentation and wizard output warn against reasoning models as pre-processors and recommend specific non-reasoning alternatives. Full test suite still passes.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify pre-processor fix via re-piloting type_a conditions</name>
  <files>none</files>
  <action>
Present the user with verification instructions for re-piloting. The user will run these commands manually since they require live API calls and cost real money.

Verification steps to present:
1. Run `propt pilot --session post-fix-type-a` to re-pilot with fixes applied
2. Run `propt report --session post-fix-type-a` to see accuracy breakdown
3. Check type_a pre_proc_sanitize accuracy >= raw accuracy
4. Check clean/type_b conditions show no preproc API calls (preproc_skipped)
5. Run `propt inspect --last --intervention pre_proc_sanitize | head -20` to verify skip behavior
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification of re-pilot results"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms type_a preproc accuracy is >= raw accuracy
    - User confirms clean/ESL conditions show preproc skipped
  </acceptance_criteria>
  <done>User has verified that re-piloting type_a conditions shows accuracy improvement or parity vs raw, and clean/ESL conditions correctly skip preprocessing.</done>
</task>

</tasks>

<verification>
- `grep "non-reasoning" docs/getting-started.md` -- model guidance present
- `grep "non-reasoning" src/setup_wizard.py` -- wizard guidance present
- `pytest tests/ -v` -- full suite green
- Human verification of re-pilot accuracy results
</verification>

<success_criteria>
- Docs clearly warn against reasoning models as pre-processors with specific alternatives listed
- Wizard output includes the same guidance during model role explanation
- (Human verified) Re-piloting type_a conditions shows accuracy improvement or parity vs raw
- (Human verified) Clean/ESL conditions show preproc skipped in inspect output
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-pre-processor-output-quality-and-performance/23-02-SUMMARY.md`
</output>
