---
phase: quick
plan: 260325-w6g
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.py
  - src/setup_wizard.py
  - src/run_experiment.py
  - src/execution_summary.py
  - tests/test_setup_wizard.py
  - tests/test_cli.py
autonomous: true
requirements: [graceful-ctrl-c]

must_haves:
  truths:
    - "Ctrl-C during setup wizard prints a clean message and exits without traceback"
    - "Ctrl-C during experiment execution closes the DB connection and prints partial progress"
    - "Ctrl-C at any confirmation prompt exits cleanly with a newline and abort message"
  artifacts:
    - path: "src/cli.py"
      provides: "Top-level KeyboardInterrupt handler wrapping main()"
    - path: "src/setup_wizard.py"
      provides: "KeyboardInterrupt handling around all input_fn() calls"
    - path: "src/run_experiment.py"
      provides: "KeyboardInterrupt handler around the tqdm processing loop"
    - path: "src/execution_summary.py"
      provides: "KeyboardInterrupt handler in confirm_execution while loop"
  key_links:
    - from: "src/cli.py"
      to: "all subcommands"
      via: "try/except KeyboardInterrupt in main()"
      pattern: "except KeyboardInterrupt"
---

<objective>
Add graceful Ctrl-C (KeyboardInterrupt) handling to the setup wizard, execution engine, confirmation prompts, and CLI entry point.

Purpose: Currently, pressing Ctrl-C at any interactive prompt or during experiment execution produces an ugly Python traceback. Users should see a clean exit message instead.

Output: Updated source files with KeyboardInterrupt handlers at all user-facing input points.
</objective>

<execution_context>
@/home/steve/linguistic-tax/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/linguistic-tax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/cli.py
@src/setup_wizard.py
@src/run_experiment.py
@src/execution_summary.py
@tests/test_setup_wizard.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KeyboardInterrupt handling to CLI entry point and confirmation prompt</name>
  <files>src/cli.py, src/execution_summary.py</files>
  <action>
In `src/cli.py` `main()`: Wrap the `args.func(args)` call (and the parsing above it) in a try/except KeyboardInterrupt that prints `"\nAborted."` to stderr and calls `sys.exit(130)` (standard Unix convention for SIGINT exit code). This is the top-level catch-all so no subcommand ever produces a raw traceback from Ctrl-C.

In `src/execution_summary.py` `confirm_execution()`: Add a try/except KeyboardInterrupt around the `while True` loop (lines 323-332). On KeyboardInterrupt, print `"\nAborted."` and return `"no"`. This handles Ctrl-C specifically at the Y/N/M prompt so callers get a clean "no" decision and can close resources properly, rather than the exception propagating up to cli.py's catch-all.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -c "from src.execution_summary import confirm_execution; r = confirm_execution('test', input_fn=lambda _: (_ for _ in ()).throw(KeyboardInterrupt)); print(r)" 2>&1 | grep -q "no" && echo PASS || echo FAIL</automated>
  </verify>
  <done>Ctrl-C at any confirmation prompt returns "no" cleanly. Ctrl-C anywhere in the CLI produces "Aborted." on stderr and exits 130.</done>
</task>

<task type="auto">
  <name>Task 2: Add KeyboardInterrupt handling to setup wizard and execution engine</name>
  <files>src/setup_wizard.py, src/run_experiment.py</files>
  <action>
In `src/setup_wizard.py` `run_setup_wizard()`: Wrap the entire interactive flow (lines 198-294, everything after the non_interactive early return) in a try/except KeyboardInterrupt that prints `"\nSetup cancelled."` and returns (allowing cli.py's main to exit normally). This is cleaner than wrapping each individual input_fn() call since the wizard should abort entirely on Ctrl-C at any step.

In `src/run_experiment.py` `run_engine()`: Wrap the tqdm processing loop (the `with tqdm(...)` block, lines 504-515) in a try/except KeyboardInterrupt. On interrupt:
1. Print `"\nInterrupted. Saving progress..."` (use print, not logger, per Phase 14 decision for user-facing CLI output)
2. Close the DB connection with `conn.close()`
3. Print the number of items completed out of total (use the loop variable `i` to determine progress)
4. Call `sys.exit(130)`

Do NOT re-raise the KeyboardInterrupt -- the handler should cleanly exit. The DB connection close ensures SQLite commits are flushed. Completed items are already committed per-item via `insert_run()`, so partial progress is preserved automatically.
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -c "
import ast, sys
# Verify KeyboardInterrupt appears in both files
for path in ['src/setup_wizard.py', 'src/run_experiment.py']:
    with open(path) as f:
        tree = ast.parse(f.read())
    handlers = [n for n in ast.walk(tree) if isinstance(n, ast.ExceptHandler) and n.type and getattr(n.type, 'id', '') == 'KeyboardInterrupt']
    if not handlers:
        print(f'FAIL: {path} missing KeyboardInterrupt handler')
        sys.exit(1)
    print(f'PASS: {path} has {len(handlers)} KeyboardInterrupt handler(s)')
"</automated>
  </verify>
  <done>Ctrl-C during setup wizard prints "Setup cancelled." and exits cleanly. Ctrl-C during experiment execution saves progress, closes DB, and exits with code 130.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for KeyboardInterrupt behavior</name>
  <files>tests/test_setup_wizard.py, tests/test_cli.py</files>
  <action>
In `tests/test_setup_wizard.py`, add a test `test_wizard_ctrl_c_cancels_cleanly`:
- Create a mock `input_fn` that raises `KeyboardInterrupt` on first call
- Call `run_setup_wizard(args, input_fn=mock_input_fn)` where args has `non_interactive=False`
- Assert the function returns `None` (does not raise)
- Assert "Setup cancelled." appears in captured stdout (use capsys)

In `tests/test_cli.py`, add a test `test_main_ctrl_c_exits_130`:
- Monkeypatch `argparse.ArgumentParser.parse_args` to raise `KeyboardInterrupt`
- Call `main()` and assert it raises `SystemExit` with code 130
- Assert "Aborted." appears in captured stderr (use capsys)

Also add a test `test_confirm_execution_ctrl_c_returns_no` in the appropriate test file (test_cli.py or create inline):
- Call `confirm_execution("summary", input_fn=lambda _: (_ for _ in ()).throw(KeyboardInterrupt))`
- Assert return value is `"no"`
- Assert "Aborted." appears in captured stdout
  </action>
  <verify>
    <automated>cd /home/steve/linguistic-tax && python -m pytest tests/test_setup_wizard.py::test_wizard_ctrl_c_cancels_cleanly tests/test_cli.py::test_main_ctrl_c_exits_130 tests/test_cli.py::test_confirm_execution_ctrl_c_returns_no -v</automated>
  </verify>
  <done>All three KeyboardInterrupt tests pass, confirming clean exit behavior for wizard, CLI main, and confirmation prompt.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_setup_wizard.py tests/test_cli.py -v` -- all tests pass including new Ctrl-C tests
2. `python -m pytest tests/ -v` -- no regressions in full test suite
3. Manual spot-check: `python -m src.cli setup` then Ctrl-C shows "Setup cancelled." with no traceback
</verification>

<success_criteria>
- KeyboardInterrupt at any input prompt produces a clean message, no traceback
- Experiment execution Ctrl-C closes DB and reports partial progress
- CLI exit code is 130 (Unix SIGINT convention) for all Ctrl-C exits
- All existing tests continue to pass
- Three new tests verify the KeyboardInterrupt behavior
</success_criteria>

<output>
After completion, create `.planning/quick/260325-w6g-support-ctrl-c-in-the-wizard-and-scripts/260325-w6g-SUMMARY.md`
</output>
