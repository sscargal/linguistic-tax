---
phase: quick-260329-30q
plan: 01
subsystem: experiment-execution
tags: [deduplication, setup-wizard, matrix-remap]

requires:
  - phase: none
    provides: n/a
provides:
  - "Deduplicated _remap_matrix_models with noise_level in dedup key"
  - "Same-model preproc/target warning in setup wizard"
affects: [pilot, setup-wizard, run-experiment]

tech-stack:
  added: []
  patterns:
    - "dict.fromkeys() for order-preserving dedup of model lists"

key-files:
  created: []
  modified:
    - src/pilot.py
    - src/setup_wizard.py
    - tests/test_pilot.py
    - tests/test_setup_wizard.py

key-decisions:
  - "Used dict.fromkeys() for order-preserving dedup instead of set()"
  - "noise_level stored as str() in dedup key for consistent hashing"

patterns-established: []

requirements-completed: [DEDUP-01, DETECT-01]

duration: 2min
completed: 2026-03-29
---

# Quick Task 260329-30q: Deduplicate Matrix Model Remap and Detect Same-Model Preproc Summary

**Fixed _remap_matrix_models to deduplicate configured models and include noise_level in dedup key; added same-model preproc/target warning in setup wizard**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-29T02:13:53Z
- **Completed:** 2026-03-29T02:16:17Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- _remap_matrix_models now deduplicates configured_models list before expansion, preventing wasted duplicate API calls
- Added noise_level to the dedup key tuple, fixing a latent bug where items differing only by noise_level would be collapsed
- Setup wizard now warns when user picks the same model as both target and pre-processor

## Task Commits

Each task was committed atomically (TDD: test then feat):

1. **Task 1 RED: Failing dedup tests** - `40efbb3` (test)
2. **Task 1 GREEN: Fix _remap_matrix_models** - `69456b1` (feat)
3. **Task 2 RED: Failing same-model warning tests** - `6e52e9d` (test)
4. **Task 2 GREEN: Add wizard warning** - `7ca8fbe` (feat)

## Files Created/Modified
- `src/pilot.py` - Deduplicate configured_models, add noise_level to dedup key, log warning on dedup
- `src/setup_wizard.py` - Print warning when preproc_model == target_model
- `tests/test_pilot.py` - 4 new tests in TestRemapMatrixModels class
- `tests/test_setup_wizard.py` - 2 new tests in TestSelectSingleTargetSameModelWarning class

## Decisions Made
- Used `dict.fromkeys()` for order-preserving deduplication instead of `set()` to maintain deterministic model ordering
- Converted noise_level to `str()` in the dedup key for consistent hashing (handles None and numeric values)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Dedup and warning are self-contained fixes with no downstream dependencies

---
*Phase: quick-260329-30q*
*Completed: 2026-03-29*
